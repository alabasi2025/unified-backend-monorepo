import { Controller, Get, Param, Body, Query } from '@nestjs/common';

@Controller('dashboard')
export class DashboardController {
  // يمكن حقن الخدمات هنا عبر constructor
  // constructor(private readonly dashboardService: DashboardService) {}

  @Get('stats')
  getStats() {
    // منطق جلب الإحصائيات
    return { message: 'Dashboard statistics' };
  }

  @Get('recent')
  getRecent() {
    // منطق جلب العناصر الحديثة
    return { message: 'Recent items' };
  }

  @Get('recommendations')
  getRecommendations() {
    // منطق جلب التوصيات
    return { message: 'Recommendations' };
  }
}
