import { Controller, Get, Query } from '@nestjs/common';

@Controller('search')
export class SearchController {
  // يمكن إضافة خدمة (Service) هنا إذا لزم الأمر
  // constructor(private readonly searchService: SearchService) {}

  /**
   * GET /search
   * البحث العام
   */
  @Get()
  searchAll(@Query('q') query: string) {
    // منطق البحث العام
    return { message: `Searching all entities for query: ${query}` };
  }

  /**
   * GET /search/notebooks
   * البحث في الدفاتر
   */
  @Get('notebooks')
  searchNotebooks(@Query('q') query: string) {
    // منطق البحث في الدفاتر
    return { message: `Searching notebooks for query: ${query}` };
  }

  /**
   * GET /search/pages
   * البحث في الصفحات
   */
  @Get('pages')
  searchPages(@Query('q') query: string) {
    // منطق البحث في الصفحات
    return { message: `Searching pages for query: ${query}` };
  }

  /**
   * GET /search/ideas
   * البحث في الأفكار
   */
  @Get('ideas')
  searchIdeas(@Query('q') query: string) {
    // منطق البحث في الأفكار
    return { message: `Searching ideas for query: ${query}` };
  }

  /**
   * GET /search/tasks
   * البحث في المهام
   */
  @Get('tasks')
  searchTasks(@Query('q') query: string) {
    // منطق البحث في المهام
    return { message: `Searching tasks for query: ${query}` };
  }

  /**
   * GET /search/reports
   * البحث في التقارير
   */
  @Get('reports')
  searchReports(@Query('q') query: string) {
    // منطق البحث في التقارير
    return { message: `Searching reports for query: ${query}` };
  }
}
