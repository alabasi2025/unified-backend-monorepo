# دفتر التوثيق الذكي الشامل
# SEMOP - Smart Enterprise Management & Operations Platform

**الإصدار:** 2.0.0  
**التاريخ:** 22 نوفمبر 2025  
**الحالة:** Production Ready  
**المؤلف:** Manus AI

---

## 📚 جدول المحتويات

### القسم الأول: نظرة عامة
1. [مقدمة عن SEMOP](#1-مقدمة-عن-semop)
2. [الرؤية والأهداف](#2-الرؤية-والأهداف)
3. [المميزات الرئيسية](#3-المميزات-الرئيسية)
4. [البنية التقنية](#4-البنية-التقنية)

### القسم الثاني: البنية المعمارية
5. [الفلسفة المعمارية](#5-الفلسفة-المعمارية)
6. [البنية الطبقية الرباعية](#6-البنية-الطبقية-الرباعية)
7. [الطبقة 1: خدمات النواة](#7-الطبقة-1-خدمات-النواة)
8. [الطبقة 2: منصة التحكم التشغيلي](#8-الطبقة-2-منصة-التحكم-التشغيلي)
9. [الطبقة 3: التطبيقات الرأسية](#9-الطبقة-3-التطبيقات-الرأسية)
10. [الطبقة 4: المكتبات القطاعية](#10-الطبقة-4-المكتبات-القطاعية)

### القسم الثالث: قاعدة البيانات
11. [نظام الكيانات المتعددة](#11-نظام-الكيانات-المتعددة)
12. [نظام الهوية والصلاحيات](#12-نظام-الهوية-والصلاحيات)
13. [نظام المحاسبة](#13-نظام-المحاسبة)
14. [نظام المخزون](#14-نظام-المخزون)
15. [نظام المشتريات والمبيعات](#15-نظام-المشتريات-والمبيعات)
16. [Prisma ORM والتكامل](#16-prisma-orm-والتكامل)

### القسم الرابع: Smart Notebook
17. [مقدمة عن Smart Notebook](#17-مقدمة-عن-smart-notebook)
18. [وحدة الأفكار (Ideas)](#18-وحدة-الأفكار-ideas)
19. [وحدة سجلات المحادثات (ChatLogs)](#19-وحدة-سجلات-المحادثات-chatlogs)
20. [وحدة التقارير (Reports)](#20-وحدة-التقارير-reports)
21. [وحدة المهام (Tasks)](#21-وحدة-المهام-tasks)
22. [وحدة الصفحات (Pages)](#22-وحدة-الصفحات-pages)
23. [التكامل مع AI](#23-التكامل-مع-ai)

### القسم الخامس: نظام الخرائط
24. [Maps System Overview](#24-maps-system-overview)
25. [التكامل مع Google Maps](#25-التكامل-مع-google-maps)
26. [تتبع الأصول والموظفين](#26-تتبع-الأصول-والموظفين)
27. [حالات الاستخدام](#27-حالات-الاستخدام)

### القسم السادس: APIs والتكامل
28. [بنية REST APIs](#28-بنية-rest-apis)
29. [المصادقة والتفويض](#29-المصادقة-والتفويض)
30. [قائمة Endpoints الكاملة](#30-قائمة-endpoints-الكاملة)
31. [أمثلة الاستخدام](#31-أمثلة-الاستخدام)
32. [معالجة الأخطاء](#32-معالجة-الأخطاء)

### القسم السابع: دليل المطورين
33. [إعداد بيئة التطوير](#33-إعداد-بيئة-التطوير)
34. [هيكل المشروع](#34-هيكل-المشروع)
35. [معايير الكود](#35-معايير-الكود)
36. [إضافة وحدة جديدة](#36-إضافة-وحدة-جديدة)
37. [الاختبارات](#37-الاختبارات)
38. [Git Workflow](#38-git-workflow)

### القسم الثامن: النشر والصيانة
39. [متطلبات السيرفر](#39-متطلبات-السيرفر)
40. [عملية النشر](#40-عملية-النشر)
41. [إدارة PM2](#41-إدارة-pm2)
42. [النسخ الاحتياطي](#42-النسخ-الاحتياطي)
43. [المراقبة والتنبيهات](#43-المراقبة-والتنبيهات)
44. [استكشاف الأخطاء](#44-استكشاف-الأخطاء)

### القسم التاسع: الأمان
45. [سياسات الأمان](#45-سياسات-الأمان)
46. [إدارة الأسرار](#46-إدارة-الأسرار)
47. [SSL/TLS](#47-ssltls)
48. [معايير OWASP](#48-معايير-owasp)

### القسم العاشر: الملاحق
49. [المصطلحات](#49-المصطلحات)
50. [الأسئلة الشائعة](#50-الأسئلة-الشائعة)
51. [سجل التغييرات](#51-سجل-التغييرات)
52. [المراجع](#52-المراجع)

---

## القسم الأول: نظرة عامة

### 1. مقدمة عن SEMOP

**SEMOP** (Smart Enterprise Management & Operations Platform) هو نظام ERP متكامل مصمم خصيصاً للشركات والمؤسسات في الشرق الأوسط، مع تركيز خاص على السوق اليمني. يجمع النظام بين قوة أنظمة ERP التقليدية ومرونة الحلول السحابية الحديثة، مع إضافة ميزات ذكية مدعومة بالذكاء الاصطناعي.

#### الخصائص الأساسية

يتميز SEMOP بمجموعة من الخصائص التي تجعله فريداً في السوق:

**الدعم الكامل للغة العربية**: تم تصميم النظام من الأساس ليدعم اللغة العربية بشكل كامل، ليس فقط في واجهة المستخدم، بل أيضاً في قاعدة البيانات والتقارير والوثائق. جميع الجداول تحتوي على حقول `nameAr` و `nameEn` لضمان تجربة سلسة للمستخدمين العرب.

**البنية المعيارية (Modular Architecture)**: يتبع SEMOP نهجاً معيارياً فريداً حيث يمكن تركيب وتشغيل كل وحدة بشكل مستقل. هذا يعني أن العميل يمكنه البدء بوحدة المحاسبة فقط، ثم إضافة وحدة المخزون لاحقاً، دون الحاجة لإعادة تهيئة النظام بالكامل.

**التكامل مع الأجهزة والأنظمة الخارجية**: من خلال منصة التحكم التشغيلي (OCMP - Operational Control & Monitoring Platform)، يمكن لـ SEMOP الاتصال مباشرة بأجهزة IoT، كاميرات المراقبة، أنظمة نقاط البيع (POS)، وحتى أجهزة الاستشعار الصناعية.

**الذكاء الاصطناعي المدمج**: يحتوي SEMOP على وحدة Smart Notebook التي تستخدم نماذج اللغة الكبيرة (LLMs) لمساعدة المستخدمين في تحليل البيانات، إنشاء التقارير، وتقديم توصيات ذكية.

#### الجمهور المستهدف

تم تصميم SEMOP لخدمة مجموعة واسعة من المؤسسات:

**الشركات الصغيرة والمتوسطة**: التي تحتاج إلى نظام ERP قوي لكن بتكلفة معقولة وسهولة في الاستخدام. يمكن لهذه الشركات البدء بالوحدات الأساسية فقط وتوسيع النظام مع نمو أعمالها.

**الشركات القابضة**: التي تدير عدة وحدات أو فروع أو شركات تابعة. نظام الكيانات المتعددة (Multi-Entity System) في SEMOP يسمح بإدارة مركزية مع استقلالية كاملة لكل كيان.

**المؤسسات الصناعية**: التي تحتاج إلى تتبع دقيق للإنتاج، المخزون، والأصول. تكامل SEMOP مع أجهزة IoT يوفر رؤية فورية لحالة المعدات والإنتاج.

**قطاع التجزئة**: من خلال تكامل نقاط البيع، إدارة المخزون الذكية، ونظام الولاء للعملاء.

---

### 2. الرؤية والأهداف

#### الرؤية

أن نكون المنصة الرائدة لإدارة المؤسسات في الشرق الأوسط، من خلال توفير حلول ذكية ومرنة تمكّن الشركات من النمو والازدهار في العصر الرقمي.

#### الأهداف الاستراتيجية

**تبسيط إدارة الأعمال**: جعل عمليات الإدارة اليومية أسهل وأسرع من خلال أتمتة المهام المتكررة وتوفير واجهات بديهية.

**تمكين اتخاذ القرارات المبنية على البيانات**: من خلال توفير تقارير شاملة، لوحات معلومات تفاعلية، وتحليلات ذكية مدعومة بالذكاء الاصطناعي.

**دعم النمو المستدام**: بنية معيارية تنمو مع نمو الشركة، دون الحاجة للانتقال إلى نظام آخر.

**الامتثال للمعايير المحلية**: دعم كامل للمتطلبات المحاسبية والضريبية في دول الشرق الأوسط.

---

### 3. المميزات الرئيسية

#### 3.1 نظام الكيانات المتعددة (Multi-Entity System)

واحدة من أقوى ميزات SEMOP هي القدرة على إدارة هيكل تنظيمي معقد من خلال نظام الكيانات المتعددة. يتكون الهيكل من ثلاث مستويات:

**الشركة القابضة (Holding)**: المستوى الأعلى في الهيكل، يمثل الكيان الأم الذي يملك أو يدير عدة وحدات. مثال: "مجموعة الباسي القابضة".

**الوحدة (Unit)**: المستوى الثاني، يمكن أن تكون فرعاً، قسماً، شعبة، أو شركة تابعة. مثال: "فرع صنعاء"، "قسم المبيعات"، "شركة الباسي للتجارة".

**المشروع (Project)**: المستوى الثالث، يمثل مشاريع محددة تُدار داخل الوحدات. مثال: "مشروع تطوير نظام ERP"، "مشروع افتتاح فرع جديد".

هذا الهيكل يسمح بـ:
- **إدارة مركزية**: رؤية شاملة لجميع الكيانات من لوحة تحكم واحدة
- **استقلالية الوحدات**: كل وحدة يمكنها العمل بشكل مستقل مع بياناتها الخاصة
- **تقارير موحدة**: إمكانية إنشاء تقارير موحدة على مستوى الشركة القابضة أو تقارير منفصلة لكل وحدة
- **صلاحيات دقيقة**: تحديد من يمكنه الوصول إلى بيانات أي كيان

#### 3.2 Smart Notebook - دفتر الملاحظات الذكي

Smart Notebook هو نظام فريد يجمع بين إدارة المعرفة والذكاء الاصطناعي. يتكون من خمس وحدات رئيسية:

**وحدة الأفكار (Ideas)**: لتسجيل وتتبع الأفكار والمبادرات. يمكن للمستخدمين تسجيل أفكارهم، تصنيفها، وتحويلها إلى مهام أو مشاريع.

**وحدة سجلات المحادثات (ChatLogs)**: تحفظ جميع المحادثات مع نماذج الذكاء الاصطناعي، مما يسمح بالرجوع إليها لاحقاً والبناء على التحليلات السابقة.

**وحدة التقارير (Reports)**: إنشاء تقارير ذكية مدعومة بالذكاء الاصطناعي. يمكن للنظام تحليل البيانات وإنشاء تقارير شاملة بلغة طبيعية.

**وحدة المهام (Tasks)**: إدارة المهام المرتبطة بالأفكار والتقارير، مع تتبع الحالة والأولوية.

**وحدة الصفحات (Pages)**: نظام wiki داخلي لتوثيق المعرفة المؤسسية، مع دعم Markdown وإمكانية الربط بين الصفحات.

#### 3.3 نظام الخرائط المتقدم

نظام خرائط متكامل يوفر:
- **تتبع الأصول في الوقت الفعلي**: معرفة موقع المركبات، المعدات، والأصول المتحركة على الخريطة
- **تتبع الموظفين**: لفرق المبيعات والتوصيل والصيانة الميدانية
- **تحديد المناطق**: رسم مناطق جغرافية وتعيين الموظفين أو الأصول لها
- **تحليل المسارات**: تحليل مسارات الحركة وتحسين الطرق
- **خرائط أوفلاين**: عمل الخرائط حتى بدون اتصال بالإنترنت (خاص باليمن)

#### 3.4 نظام المحاسبة الشامل

نظام محاسبي كامل يتوافق مع المعايير الدولية ويدعم:
- **دليل حسابات مرن**: هيكل شجري متعدد المستويات
- **القيود اليومية**: إنشاء وإدارة القيود المحاسبية
- **الفترات المالية**: إدارة السنوات والفترات المالية
- **التقارير المالية**: الميزانية، قائمة الدخل، التدفقات النقدية
- **مراكز التكلفة**: تتبع التكاليف على مستوى المشاريع والأقسام

#### 3.5 إدارة المخزون الذكية

- **تتبع دقيق**: رقم تسلسلي أو دفعات (Batches) لكل منتج
- **مستودعات متعددة**: إدارة عدة مستودعات مع تتبع الحركة بينها
- **الجرد الدوري**: جداول جرد منتظمة مع تسجيل الفروقات
- **تنبيهات ذكية**: تنبيهات عند الوصول لحد إعادة الطلب
- **تكامل مع المبيعات والمشتريات**: تحديث تلقائي للمخزون

---

### 4. البنية التقنية

#### 4.1 المكدس التقني (Technology Stack)

**Backend:**
- **NestJS**: إطار عمل Node.js قوي ومعياري
- **TypeScript**: للكتابة الآمنة من الأخطاء
- **Prisma ORM**: للتعامل مع قاعدة البيانات
- **PostgreSQL**: قاعدة بيانات علائقية قوية
- **JWT**: للمصادقة والتفويض

**Frontend:**
- **Angular**: إطار عمل شامل لبناء تطبيقات الويب
- **TypeScript**: لضمان جودة الكود
- **RxJS**: للبرمجة التفاعلية
- **Angular Material**: مكتبة مكونات UI

**DevOps:**
- **Nx Monorepo**: لإدارة المشروع الضخم
- **PM2**: لإدارة العمليات في Production
- **GitHub**: للتحكم بالإصدارات
- **Hostinger**: استضافة Production

**قاعدة البيانات:**
- **PostgreSQL 14+**: قاعدة البيانات الرئيسية
- **Prisma Migrations**: لإدارة تغييرات قاعدة البيانات

#### 4.2 معمارية Nx Monorepo

SEMOP مبني على Nx Monorepo، مما يوفر:

**إدارة موحدة**: جميع المكتبات والتطبيقات في مستودع واحد
**مشاركة الكود**: مكتبات مشتركة بين Frontend و Backend
**بناء ذكي**: Nx يبني فقط ما تغير
**اختبارات موحدة**: معايير اختبار موحدة عبر المشروع
**سهولة التطوير**: أدوات CLI قوية لإنشاء وإدارة المكونات

#### 4.3 البنية المعمارية العامة

```
┌─────────────────────────────────────────────────────────┐
│                    Angular Frontend                      │
│              (alabasi.semop.org)                        │
└────────────────────┬────────────────────────────────────┘
                     │ HTTP/REST
                     ▼
┌─────────────────────────────────────────────────────────┐
│                  NestJS API Gateway                      │
│            (alabasi.semop.org/api)                      │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Smart Notebook  │  Auth  │  Maps  │  Core APIs │  │
│  └──────────────────────────────────────────────────┘  │
└────────────────────┬────────────────────────────────────┘
                     │ Prisma ORM
                     ▼
┌─────────────────────────────────────────────────────────┐
│              PostgreSQL Database                         │
│     (50+ Tables, 1000+ Columns)                         │
└─────────────────────────────────────────────────────────┘
```

---

**تابع في الأقسام التالية...**



## القسم الثاني: البنية المعمارية

### 5. الفلسفة المعمارية

تقوم البنية المعمارية لـ SEMOP على فلسفة فريدة تجمع بين قوة الأنظمة المعيارية ومرونة الخدمات الصغيرة (Microservices)، مع الحفاظ على بساطة التطوير والنشر.

#### 5.1 المبادئ الأساسية

**كل شيء هو مكتبة (Everything is a Library)**: بدلاً من بناء تطبيق ضخم واحد (Monolith)، يتم تقسيم كل منطق عمل إلى مكتبة معزولة داخل Nx Monorepo. هذا يعني أن كل وحدة وظيفية (مثل المحاسبة، المخزون، أو الفوترة) هي مكتبة مستقلة يمكن تطويرها واختبارها ونشرها بشكل منفصل.

**البنية الطبقية الرباعية**: تم تقسيم النظام إلى أربع طبقات وظيفية واضحة ومحددة. كل طبقة لها مسؤوليات محددة ولا يمكنها تجاوز حدودها. هذا التقسيم يضمن وضوح المسؤوليات ويسهل الصيانة والتطوير.

**العزل والاستقلالية**: كل مكتبة مستقلة تماماً عن المكتبات الأخرى في نفس الطبقة. المكتبة يمكنها فقط الاعتماد على المكتبات في الطبقات الأدنى، ولا يمكنها أبداً الاعتماد على مكتبات في طبقات أعلى. هذا يمنع التشابك والاعتماديات الدائرية (Circular Dependencies).

**قابلية التوسع اللانهائية**: البنية مصممة بحيث يمكن تحويل أي مكتبة إلى Microservice منفصل في المستقبل دون الحاجة لإعادة كتابة الكود. إذا نما النظام وأصبح يخدم آلاف المستخدمين، يمكن فصل وحدة المحاسبة مثلاً إلى خدمة مستقلة تعمل على سيرفر منفصل.

#### 5.2 لماذا Nx Monorepo؟

اختيار Nx Monorepo لم يكن عشوائياً، بل جاء بعد دراسة متأنية للبدائل:

**مقارنة مع Polyrepo (مستودعات متعددة)**:
- في Polyrepo، كل مكتبة في مستودع منفصل، مما يجعل مشاركة الكود معقدة
- تحديث مكتبة مشتركة يتطلب تحديث كل المستودعات التي تستخدمها
- الاختبارات المتكاملة (Integration Tests) صعبة جداً

**مقارنة مع Monolith (تطبيق واحد ضخم)**:
- في Monolith، كل الكود في تطبيق واحد، مما يجعل الصيانة صعبة
- لا يمكن إعادة استخدام الكود بسهولة
- النشر يجب أن يكون للنظام بالكامل، حتى لو تغير جزء صغير

**مزايا Nx Monorepo**:
- **مشاركة الكود**: مكتبات مشتركة بين Frontend و Backend
- **بناء ذكي**: Nx يبني فقط ما تغير، مما يوفر الوقت
- **اختبارات موحدة**: معايير اختبار موحدة عبر المشروع
- **أدوات قوية**: CLI قوي لإنشاء وإدارة المكونات
- **رسم بياني للاعتماديات**: Nx يوفر رسماً بيانياً تفاعلياً يوضح كيف ترتبط المكتبات ببعضها

---

### 6. البنية الطبقية الرباعية

البنية الطبقية هي العمود الفقري لـ SEMOP. كل طبقة لها دور محدد ومسؤوليات واضحة.

```
┌─────────────────────────────────────────────────────────┐
│  الطبقة 4: المكتبات القطاعية (Sector Libraries)        │
│  energy-sector, retail-sector, healthcare-sector...    │
└────────────────────┬────────────────────────────────────┘
                     │ depends on
                     ▼
┌─────────────────────────────────────────────────────────┐
│  الطبقة 3: التطبيقات الرأسية (Vertical Applications)    │
│  accounting, inventory, crm, assets, tasks...          │
└────────────────────┬────────────────────────────────────┘
                     │ depends on
                     ▼
┌─────────────────────────────────────────────────────────┐
│  الطبقة 2: منصة التحكم التشغيلي (OCMP)                  │
│  iot-gateway, hardware-adapters, network-adapters      │
└────────────────────┬────────────────────────────────────┘
                     │ depends on
                     ▼
┌─────────────────────────────────────────────────────────┐
│  الطبقة 1: خدمات النواة (Core Services)                 │
│  multi-entity, identity, configuration, billing...     │
└─────────────────────────────────────────────────────────┘
```

#### 6.1 قواعد الاعتماد

**القاعدة الذهبية**: لا يمكن لطبقة أدنى أن تعتمد على طبقة أعلى. هذا يضمن:
- **وضوح المسؤوليات**: كل طبقة تعرف بالضبط ما يمكنها فعله
- **سهولة الاختبار**: يمكن اختبار كل طبقة بشكل مستقل
- **قابلية إعادة الاستخدام**: الطبقات الأدنى يمكن استخدامها في مشاريع أخرى
- **منع التشابك**: لا توجد اعتماديات دائرية

**مثال عملي**:
- مكتبة `accounting` (الطبقة 3) يمكنها استخدام `multi-entity` (الطبقة 1)
- مكتبة `energy-sector` (الطبقة 4) يمكنها استخدام `accounting` (الطبقة 3)
- لكن `multi-entity` (الطبقة 1) **لا يمكنها** استخدام `accounting` (الطبقة 3)

---

### 7. الطبقة 1: خدمات النواة

خدمات النواة هي الأساس الذي يُبنى عليه كل شيء آخر. هذه الخدمات ضرورية لعمل أي نظام ERP.

#### 7.1 Multi-Entity Service

**المشكلة التي يحلها**: كيف يمكن لشركة قابضة تمتلك عدة وحدات ومشاريع أن تدير كل شيء من نظام واحد؟

**الحل**: نظام الكيانات المتعددة الهرمي:

```
الشركة القابضة (Holding)
  ├── الوحدة 1 (Unit)
  │   ├── المشروع A (Project)
  │   └── المشروع B (Project)
  ├── الوحدة 2 (Unit)
  │   └── المشروع C (Project)
  └── الوحدة 3 (Unit)
```

**الميزات الرئيسية**:

**عزل البيانات**: كل كيان له بياناته الخاصة المعزولة. مستخدم في الوحدة 1 لا يمكنه رؤية بيانات الوحدة 2 إلا إذا مُنح صلاحية صريحة.

**المعاملات البينية**: يمكن للوحدات والمشاريع التعامل مع بعضها البعض. مثلاً، الوحدة 1 يمكنها بيع منتجات للوحدة 2، والنظام يسجل ذلك كمعاملة بينية (Inter-Entity Transaction).

**التقارير الموحدة**: يمكن للإدارة العليا رؤية تقارير موحدة على مستوى الشركة القابضة، بينما كل وحدة ترى تقاريرها الخاصة فقط.

**الكود الفريد**: كل كيان له كود فريد (مثل HOLD001، UNIT001، PROJ001) يُستخدم في جميع المعاملات.

#### 7.2 Identity and Access Management

**المشكلة التي يحلها**: من يمكنه الوصول إلى ماذا؟

**الحل**: نظام صلاحيات متقدم يجمع بين:

**Role-Based Access Control (RBAC)**: صلاحيات مبنية على الأدوار. مثلاً، دور "محاسب" له صلاحيات محددة.

**Entity-Level Permissions**: صلاحيات على مستوى الكيان. مستخدم يمكنه أن يكون "مدير" في الوحدة 1 و"مستخدم عادي" في الوحدة 2.

**Resource-Level Permissions**: صلاحيات دقيقة على مستوى الموارد. مثلاً، يمكن للمستخدم "قراءة" الفواتير لكن لا يمكنه "حذفها".

**الميزات الرئيسية**:

**JWT Authentication**: استخدام JSON Web Tokens للمصادقة. عند تسجيل الدخول، يحصل المستخدم على token صالح لمدة محددة.

**Refresh Tokens**: لتجديد الجلسة دون الحاجة لإعادة تسجيل الدخول.

**Password Policies**: سياسات كلمات المرور (الطول الأدنى، التعقيد، انتهاء الصلاحية).

**Audit Trail**: تسجيل كل عمليات تسجيل الدخول والخروج والتغييرات في الصلاحيات.

#### 7.3 Configuration Service

**المشكلة التي يحلها**: كيف نجعل النظام قابلاً للتخصيص دون تغيير الكود؟

**الحل**: نظام تكوين ديناميكي يسمح بتفعيل/تعطيل الميزات لكل عميل.

**مثال عملي**:
- العميل A يحتاج فقط المحاسبة والمخزون
- العميل B يحتاج المحاسبة + المخزون + إدارة الأصول + نظام الطاقة
- العميل C يحتاج كل شيء

بدلاً من بناء نظام منفصل لكل عميل، نستخدم Configuration Service لتحديد الميزات المفعلة.

**الميزات الرئيسية**:

**Customer Blueprints**: مخطط لكل عميل يحدد الميزات المفعلة.

**Feature Flags**: أعلام لتفعيل/تعطيل ميزات معينة.

**Dynamic Configuration**: يمكن تغيير التكوين دون إعادة نشر النظام.

**Configuration Versioning**: تتبع التغييرات في التكوين عبر الزمن.

#### 7.4 Billing Engine

**المشكلة التي يحلها**: كيف ننشئ فواتير لأنواع مختلفة من الخدمات والمنتجات؟

**الحل**: محرك فوترة عام يدعم أنواع متعددة من الفواتير.

**أنواع الفواتير المدعومة**:

**فواتير العدادات (Meter-Based Billing)**: للطاقة، المياه، الغاز. يتم حساب الفاتورة بناءً على قراءة العداد.

**فواتير الخدمات (Service-Based Billing)**: للاشتراكات الشهرية، الصيانة، الاستشارات.

**فواتير المنتجات (Product-Based Billing)**: للمبيعات العادية.

**الميزات الرئيسية**:

**حساب الضرائب**: دعم أنواع متعددة من الضرائب (VAT، Sales Tax).

**الخصومات**: خصومات نسبية أو ثابتة، على مستوى الصنف أو الفاتورة.

**الشرائح (Tiers)**: للفوترة المتدرجة (مثل أسعار الكهرباء حسب الاستهلاك).

**التكرار التلقائي**: فواتير دورية (شهرية، ربع سنوية، سنوية).

#### 7.5 Wallet Service

**المشكلة التي يحلها**: كيف ندير أرصدة العملاء والمدفوعات الإلكترونية؟

**الحل**: محفظة إلكترونية لكل عميل.

**الميزات الرئيسية**:

**الإيداع والسحب**: إضافة أموال إلى المحفظة أو سحبها.

**الدفع من المحفظة**: دفع الفواتير مباشرة من رصيد المحفظة.

**التكامل مع بوابات الدفع**: ربط مع Stripe، PayPal، أو بوابات دفع محلية.

**سجل المعاملات**: تتبع كامل لجميع المعاملات.

**الأرصدة المتعددة**: دعم عملات متعددة في نفس المحفظة.

#### 7.6 Developer System ("أنا")

**المشكلة التي يحلها**: كيف نجعل النظام يفهم احتياجات العملاء ويطور نفسه؟

**الحل**: نظام ذكاء اصطناعي يتفاعل مع العملاء ويحول طلباتهم إلى مهام تطوير.

**كيف يعمل**:

1. **العميل يطلب ميزة**: "أريد نظام لإدارة الصيانة الوقائية"
2. **"أنا" يحلل الطلب**: يستخدم LLM لفهم المتطلبات
3. **البحث في قاعدة المعرفة**: هل هذه الميزة موجودة مسبقاً؟
4. **إنشاء بطاقة تطوير**: إذا لم تكن موجودة، ينشئ بطاقة تطوير تلقائياً
5. **تقدير الوقت والتكلفة**: يقدر الوقت اللازم للتطوير

**الميزات الرئيسية**:

**واجهة دردشة ذكية**: يمكن للعملاء التحدث مع "أنا" كأنه مطور حقيقي.

**تحليل النصوص**: فهم الطلبات المكتوبة بلغة طبيعية.

**قاعدة معرفة**: تحتوي على جميع الميزات الموجودة والوثائق.

**إدارة المهام**: تحويل الطلبات إلى مهام في نظام إدارة المشاريع.

---

### 8. الطبقة 2: منصة التحكم التشغيلي

منصة التحكم والمراقبة التشغيلية (OCMP - Operational Control & Monitoring Platform) هي الجسر بين العالم الرقمي والعالم المادي.

#### 8.1 IoT Gateway

**المشكلة التي يحلها**: كيف نستقبل البيانات من آلاف الأجهزة في الوقت الفعلي؟

**الحل**: بوابة IoT تدعم بروتوكولات متعددة.

**البروتوكولات المدعومة**:

**MQTT**: البروتوكول الأكثر شيوعاً لـ IoT. خفيف وسريع ويدعم الاتصال غير المستمر.

**HTTP/HTTPS**: للأجهزة التي تدعم REST APIs.

**WebSocket**: للاتصال ثنائي الاتجاه في الوقت الفعلي.

**CoAP**: بروتوكول خفيف للأجهزة محدودة الموارد.

**الميزات الرئيسية**:

**استقبال البيانات**: استقبال بيانات من آلاف الأجهزة في نفس الوقت.

**معالجة البيانات**: تنظيف وتحويل البيانات قبل تخزينها.

**إرسال الأوامر**: إرسال أوامر إلى الأجهزة (مثل إيقاف مولد، تشغيل مضخة).

**المراقبة**: مراقبة حالة الأجهزة والتنبيه عند حدوث مشاكل.

#### 8.2 Hardware Adapters

**المشكلة التي يحلها**: كيف نتحدث مع أجهزة صناعية تستخدم بروتوكولات قديمة؟

**الحل**: محولات (Adapters) لترجمة البروتوكولات الصناعية.

**المحولات المدعومة**:

**Modbus Adapter**: للتحدث مع المولدات، محولات الطاقة الشمسية، PLCs. Modbus هو بروتوكول صناعي قديم لكنه لا يزال مستخدماً على نطاق واسع.

**SNMP Adapter**: لقراءة بيانات الطابعات، أجهزة الشبكة، UPS. SNMP يسمح بقراءة معلومات مثل عدد الصفحات المطبوعة، مستوى الحبر، حالة الجهاز.

**ONVIF Adapter**: للتحدث مع الكاميرات وأجهزة NVR. ONVIF هو معيار مفتوح لأجهزة المراقبة.

**BACnet Adapter**: لأنظمة التحكم في المباني (HVAC، الإضاءة، الأمان).

#### 8.3 Network Adapters

**المشكلة التي يحلها**: كيف نراقب ونتحكم في الشبكة المحلية؟

**الحل**: محولات للتحدث مع أجهزة الشبكات.

**المحولات المدعومة**:

**UniFi Adapter**: للتحكم في أجهزة Ubiquiti UniFi (Access Points، Switches، Gateways). يوفر معلومات مفصلة عن الأجهزة المتصلة، استهلاك البيانات، جودة الإشارة.

**MikroTik Adapter**: للتحكم في أجهزة MikroTik Routers. يسمح بإدارة الـ Firewall، Bandwidth Management، VPN.

**ICMP Ping**: للمراقبة الأساسية. يتحقق من أن الأجهزة متصلة بالشبكة.

**الميزات الرئيسية**:

**مراقبة الأجهزة المتصلة**: معرفة من متصل بالشبكة في أي لحظة.

**إدارة Bandwidth**: تحديد سرعات الإنترنت لكل مستخدم أو جهاز.

**الأمان**: حظر أجهزة معينة، إنشاء قواعد Firewall.

**التقارير**: تقارير عن استهلاك البيانات، الأجهزة الأكثر استخداماً.

---

### 9. الطبقة 3: التطبيقات الرأسية

التطبيقات الرأسية هي الأنظمة الأساسية التي يحتاجها أي عميل، بغض النظر عن قطاعه.

#### 9.1 Accounting System

نظام محاسبي كامل يتوافق مع المعايير الدولية (IFRS) والمحلية.

**المكونات الرئيسية**:

**دليل الحسابات (Chart of Accounts)**: هيكل شجري متعدد المستويات. يمكن أن يصل إلى 10 مستويات أو أكثر.

**القيود اليومية (Journal Entries)**: نظام القيد المزدوج (Double-Entry Bookkeeping). كل معاملة لها طرف مدين وطرف دائن.

**الفترات المالية (Fiscal Periods)**: إدارة السنوات المالية والفترات (شهرية، ربع سنوية).

**مراكز التكلفة (Cost Centers)**: تتبع التكاليف على مستوى المشاريع، الأقسام، أو الأنشطة.

**التقارير المالية**:
- **الميزانية العمومية (Balance Sheet)**
- **قائمة الدخل (Income Statement)**
- **قائمة التدفقات النقدية (Cash Flow Statement)**
- **ميزان المراجعة (Trial Balance)**

**الميزات المتقدمة**:

**المعاملات البينية**: تسجيل المعاملات بين الوحدات والمشاريع.

**التسويات البنكية**: مطابقة كشوف الحساب البنكية مع السجلات.

**الإهلاك التلقائي**: حساب إهلاك الأصول الثابتة تلقائياً.

**العملات المتعددة**: دعم معاملات بعملات مختلفة مع إعادة التقييم.

#### 9.2 Inventory Management

نظام إدارة مخزون شامل يدعم عمليات معقدة.

**المكونات الرئيسية**:

**الأصناف (Items)**: تعريف المنتجات والخدمات. كل صنف له:
- كود فريد (SKU)
- وصف (عربي وإنجليزي)
- وحدات قياس (قطعة، كيلو، متر، إلخ)
- صورة
- باركود

**المستودعات (Warehouses)**: إدارة عدة مستودعات. كل مستودع له:
- موقع جغرافي
- مسؤول
- سعة تخزينية

**الحركات (Transactions)**:
- **الاستلام (Receipt)**: استلام بضاعة من مورد
- **الصرف (Issue)**: صرف بضاعة لعميل أو مشروع
- **التحويل (Transfer)**: نقل بضاعة بين مستودعات
- **التعديل (Adjustment)**: تعديل الكميات (جرد، تلف، إلخ)

**الميزات المتقدمة**:

**تتبع الدفعات (Batch Tracking)**: لكل دفعة رقم وتاريخ إنتاج وانتهاء صلاحية.

**الأرقام التسلسلية (Serial Numbers)**: لتتبع كل قطعة على حدة.

**الجرد الدوري**: جداول جرد منتظمة مع تسجيل الفروقات.

**تنبيهات ذكية**: تنبيهات عند:
- الوصول لحد إعادة الطلب (Reorder Level)
- اقتراب انتهاء الصلاحية
- توقف حركة صنف لفترة طويلة

**التكامل**: تحديث تلقائي للمخزون عند:
- إنشاء فاتورة مبيعات
- استلام أمر شراء
- صرف مواد لمشروع

#### 9.3 Supply Chain Management (SCM)

نظام إدارة المواد المستهلكة الذكية، يطبق نموذج CDAM الفريد.

**ما هو نموذج CDAM؟**

CDAM = **C**onsumable **D**ata **A**nalysis **M**odel

الفكرة: ربط الأصول بالمواد التي تستهلكها، ومقارنة الاستهلاك الفعلي (من الأجهزة) بالرصيد المصروف (من المخزون).

**مثال عملي**:

لديك طابعة متصلة بالشبكة:
1. **الاستهلاك الفعلي**: الطابعة تقول أنها طبعت 5000 صفحة هذا الشهر
2. **الرصيد المصروف**: المخزون يقول أنك صرفت خرطوشة حبر واحدة (سعة 3000 صفحة)
3. **التحليل**: هناك فرق 2000 صفحة! إما أن الخرطوشة المستخدمة أكبر، أو هناك خرطوشة أخرى لم تُسجل

**الفوائد**:

**اكتشاف الهدر**: معرفة إذا كان هناك استهلاك زائد.

**اكتشاف التسريب**: معرفة إذا كانت مواد تُستخدم دون تسجيل.

**الصيانة الوقائية**: التنبيه قبل نفاد المواد.

**تحسين الشراء**: معرفة الاستهلاك الفعلي يساعد في تحديد كميات الشراء.

#### 9.4 Assets Management

نظام إدارة الأصول الثابتة والمتحركة.

**أنواع الأصول**:

**أصول ثابتة (Fixed Assets)**: مباني، أراضي، معدات ثقيلة.

**أصول متحركة (Mobile Assets)**: مركبات، أجهزة محمولة.

**أصول مستهلكة (Consumable Assets)**: أدوات، قطع غيار.

**الميزات الرئيسية**:

**تسجيل الأصول**: كل أصل له:
- رقم فريد
- وصف
- تاريخ الشراء
- القيمة
- العمر الافتراضي
- موقع (ثابت أو متتبع بـ GPS)

**حساب الإهلاك**: عدة طرق:
- القسط الثابت (Straight-Line)
- الرصيد المتناقص (Declining Balance)
- وحدات الإنتاج (Units of Production)

**ربط مع CDAM**: ربط الأصول بالمواد التي تستهلكها.

**تتبع الموقع**: للأصول المتحركة، تتبع الموقع الجغرافي في الوقت الفعلي.

**الصيانة**: جدولة الصيانة الدورية وتسجيل أعمال الصيانة.

#### 9.5 Customer Relationship Management (CRM)

نظام إدارة علاقات العملاء.

**المكونات الرئيسية**:

**قاعدة بيانات العملاء**: معلومات كاملة عن كل عميل:
- معلومات الاتصال
- العنوان
- سجل المشتريات
- الأرصدة
- الملاحظات

**سجل التفاعلات**: تسجيل كل تفاعل مع العميل:
- المكالمات الهاتفية
- الزيارات
- الشكاوى
- الطلبات

**ربط العملاء بالأصول**: في قطاع الطاقة مثلاً، كل عميل مرتبط بعداد أو أكثر.

**التقارير**:
- أفضل العملاء (حسب المبيعات)
- العملاء غير النشطين
- تحليل سلوك العملاء

#### 9.6 Tasks and Workflows

نظام إدارة المهام والعمليات.

**المكونات الرئيسية**:

**المهام (Tasks)**: كل مهمة لها:
- عنوان ووصف
- أولوية (عالية، متوسطة، منخفضة)
- حالة (جديدة، قيد التنفيذ، مكتملة)
- مسؤول
- تاريخ استحقاق
- مرفقات

**سير العمل (Workflows)**: تسلسل آلي من المهام. مثلاً:
1. عميل يقدم طلب شراء
2. يُنشأ تلقائياً مهمة "مراجعة الطلب" للمبيعات
3. عند الموافقة، يُنشأ تلقائياً مهمة "تجهيز البضاعة" للمستودع
4. عند التجهيز، يُنشأ تلقائياً مهمة "التوصيل" للسائق

**التنبيهات والإشعارات**:
- تنبيهات عند اقتراب موعد الاستحقاق
- إشعارات عند تعيين مهمة جديدة
- تذكيرات للمهام المتأخرة

**التكامل**: يمكن إنشاء مهام تلقائياً من أي نظام آخر. مثلاً:
- عند وصول المخزون لحد إعادة الطلب، يُنشأ مهمة "طلب شراء"
- عند اقتراب موعد صيانة أصل، يُنشأ مهمة "صيانة دورية"

---

### 10. الطبقة 4: المكتبات القطاعية

المكتبات القطاعية هي "الجينات" التي تجعل SEMOP يتكيف مع احتياجات كل قطاع.

#### 10.1 Energy Sector Library

مكتبة متخصصة لقطاع الطاقة.

**المكونات الرئيسية**:

**إدارة المولدات**: التحكم في المولدات الكهربائية:
- قراءة حالة المولد (يعمل/متوقف)
- قراءة الإنتاج الحالي (kW)
- ساعات التشغيل
- استهلاك الوقود
- جدولة الصيانة

**التكامل مع الطاقة الشمسية**: قراءة بيانات من محولات الطاقة الشمسية (Inverters):
- الإنتاج اليومي
- الإنتاج الكلي
- كفاءة الألواح
- حالة البطاريات

**إدارة العدادات الذكية**: ثلاثة أنواع:
- **عدادات أوفلاين**: تُقرأ يدوياً
- **عدادات دفع مسبق**: يشحنها العميل قبل الاستخدام
- **عدادات IoT**: ترسل القراءات تلقائياً

**قواعد الفوترة الخاصة**:
- **الشرائح (Tiers)**: أسعار مختلفة حسب الاستهلاك
- **الأوقات (Time-of-Use)**: أسعار مختلفة حسب الوقت (ذروة/غير ذروة)
- **الموسمية**: أسعار مختلفة حسب الموسم (صيف/شتاء)

#### 10.2 Retail Sector Library

مكتبة متخصصة لقطاع التجزئة.

**المكونات الرئيسية**:

**منطق نقاط البيع (POS Logic)**:
- معالجة المبيعات السريعة
- دعم الباركود
- حساب الضرائب والخصومات
- طباعة الفواتير

**برامج الولاء**:
- نقاط الولاء
- الخصومات للعملاء المميزين
- العروض الخاصة

**إعادة تعبئة الأرفف التلقائية**: عند انخفاض مخزون صنف على الرف، يُنشأ تلقائياً مهمة "إعادة تعبئة".

**طباعة بطاقات الأسعار**: طباعة باركود وبطاقات أسعار للمنتجات.

**تحليل المبيعات**:
- أكثر المنتجات مبيعاً
- أوقات الذروة
- متوسط قيمة الفاتورة

---

**تابع في الأقسام التالية...**



## القسم الثالث: قاعدة البيانات

### 11. نظام الكيانات المتعددة

نظام الكيانات المتعددة هو حجر الأساس في قاعدة بيانات SEMOP. يسمح بإدارة هياكل تنظيمية معقدة من خلال ثلاث مستويات هرمية.

#### 11.1 جدول Holding (الشركة القابضة)

الشركة القابضة هي المستوى الأعلى في الهيكل التنظيمي.

**الحقول الرئيسية**:

| الحقل | النوع | الوصف |
|------|------|-------|
| id | String (CUID) | المعرف الفريد |
| code | String (Unique) | كود الشركة (مثل: HOLD001) |
| nameAr | String | الاسم بالعربية |
| nameEn | String | الاسم بالإنجليزية |
| description | String? | وصف الشركة |
| logo | String? | رابط شعار الشركة |
| isActive | Boolean | هل الشركة نشطة؟ |
| createdAt | DateTime | تاريخ الإنشاء |
| updatedAt | DateTime | تاريخ آخر تحديث |
| createdBy | String? | من أنشأ السجل |
| updatedBy | String? | من عدّل السجل |

**العلاقات**:
- **units**: علاقة واحد-لكثير مع جدول Unit
- **users**: علاقة واحد-لكثير مع جدول User

**قواعد العمل**:
- كود الشركة يجب أن يكون فريداً على مستوى النظام
- لا يمكن حذف شركة قابضة تحتوي على وحدات نشطة
- عند تعطيل شركة (isActive = false)، يتم تعطيل جميع الوحدات التابعة لها تلقائياً

#### 11.2 جدول Unit (الوحدة)

الوحدة هي المستوى الثاني، يمكن أن تكون فرعاً، قسماً، شعبة، أو شركة تابعة.

**الحقول الرئيسية**:

| الحقل | النوع | الوصف |
|------|------|-------|
| id | String (CUID) | المعرف الفريد |
| code | String (Unique) | كود الوحدة (مثل: UNIT001) |
| nameAr | String | الاسم بالعربية |
| nameEn | String | الاسم بالإنجليزية |
| description | String? | وصف الوحدة |
| type | UnitType | نوع الوحدة |
| isActive | Boolean | هل الوحدة نشطة؟ |
| holdingId | String | معرف الشركة القابضة |
| createdAt | DateTime | تاريخ الإنشاء |
| updatedAt | DateTime | تاريخ آخر تحديث |

**أنواع الوحدات (UnitType Enum)**:
- **BRANCH**: فرع
- **DEPARTMENT**: قسم
- **DIVISION**: شعبة
- **SUBSIDIARY**: شركة تابعة
- **OTHER**: أخرى

**العلاقات**:
- **holding**: علاقة كثير-لواحد مع جدول Holding
- **projects**: علاقة واحد-لكثير مع جدول Project
- **users**: علاقة واحد-لكثير مع جدول User

#### 11.3 جدول Project (المشروع)

المشروع هو المستوى الثالث، يمثل مشاريع محددة تُدار داخل الوحدات.

**الحقول الرئيسية**:

| الحقل | النوع | الوصف |
|------|------|-------|
| id | String (CUID) | المعرف الفريد |
| code | String (Unique) | كود المشروع (مثل: PROJ001) |
| nameAr | String | الاسم بالعربية |
| nameEn | String | الاسم بالإنجليزية |
| description | String? | وصف المشروع |
| startDate | DateTime | تاريخ البدء |
| endDate | DateTime? | تاريخ الانتهاء (اختياري) |
| status | ProjectStatus | حالة المشروع |
| budget | Decimal? | الميزانية المخططة |
| unitId | String | معرف الوحدة |
| managerId | String? | معرف مدير المشروع |

**حالات المشروع (ProjectStatus Enum)**:
- **PLANNING**: في مرحلة التخطيط
- **ACTIVE**: نشط
- **ON_HOLD**: متوقف مؤقتاً
- **COMPLETED**: مكتمل
- **CANCELLED**: ملغي

**العلاقات**:
- **unit**: علاقة كثير-لواحد مع جدول Unit
- **manager**: علاقة كثير-لواحد مع جدول User
- **costCenters**: علاقة واحد-لكثير مع جدول CostCenter

---

### 12. نظام الهوية والصلاحيات

نظام متقدم للمصادقة والتفويض يجمع بين RBAC (Role-Based Access Control) وصلاحيات على مستوى الكيان.

#### 12.1 جدول User (المستخدم)

**الحقول الرئيسية**:

| الحقل | النوع | الوصف |
|------|------|-------|
| id | String (CUID) | المعرف الفريد |
| username | String (Unique) | اسم المستخدم |
| email | String (Unique) | البريد الإلكتروني |
| passwordHash | String | كلمة المرور المشفرة |
| firstName | String | الاسم الأول |
| lastName | String | الاسم الأخير |
| phone | String? | رقم الهاتف |
| isActive | Boolean | هل المستخدم نشط؟ |
| lastLoginAt | DateTime? | آخر تسجيل دخول |
| holdingId | String? | الشركة القابضة الافتراضية |
| unitId | String? | الوحدة الافتراضية |
| createdAt | DateTime | تاريخ الإنشاء |
| updatedAt | DateTime | تاريخ آخر تحديث |

**العلاقات**:
- **holding**: علاقة كثير-لواحد مع Holding
- **unit**: علاقة كثير-لواحد مع Unit
- **userRoles**: علاقة واحد-لكثير مع UserRole
- **managedProjects**: المشاريع التي يديرها

#### 12.2 جدول Role (الدور)

**الحقول الرئيسية**:

| الحقل | النوع | الوصف |
|------|------|-------|
| id | String (CUID) | المعرف الفريد |
| code | String (Unique) | كود الدور (مثل: ADMIN) |
| nameAr | String | الاسم بالعربية |
| nameEn | String | الاسم بالإنجليزية |
| description | String? | وصف الدور |
| level | Int | المستوى الهرمي (1-10) |
| isSystem | Boolean | هل هو دور نظام؟ |

**الأدوار النظامية**:
- **SUPER_ADMIN**: مدير النظام الأعلى
- **HOLDING_ADMIN**: مدير الشركة القابضة
- **UNIT_ADMIN**: مدير الوحدة
- **PROJECT_MANAGER**: مدير المشروع
- **ACCOUNTANT**: محاسب
- **INVENTORY_MANAGER**: مدير مخزون
- **USER**: مستخدم عادي

#### 12.3 جدول Permission (الصلاحية)

**الحقول الرئيسية**:

| الحقل | النوع | الوصف |
|------|------|-------|
| id | String (CUID) | المعرف الفريد |
| code | String (Unique) | كود الصلاحية |
| resource | String | المورد (مثل: invoices) |
| action | PermissionAction | الإجراء |
| description | String? | وصف الصلاحية |

**أنواع الإجراءات (PermissionAction Enum)**:
- **CREATE**: إنشاء
- **READ**: قراءة
- **UPDATE**: تحديث
- **DELETE**: حذف
- **APPROVE**: اعتماد
- **EXPORT**: تصدير

**أمثلة على الصلاحيات**:
- `invoices.create`: إنشاء فواتير
- `invoices.read`: قراءة الفواتير
- `invoices.approve`: اعتماد الفواتير
- `reports.export`: تصدير التقارير

#### 12.4 جدول UserRole (ربط المستخدم بالدور)

هذا الجدول يربط المستخدمين بالأدوار على مستوى كيانات محددة.

**الحقول الرئيسية**:

| الحقل | النوع | الوصف |
|------|------|-------|
| id | String (CUID) | المعرف الفريد |
| userId | String | معرف المستخدم |
| roleId | String | معرف الدور |
| holdingId | String? | الشركة القابضة (اختياري) |
| unitId | String? | الوحدة (اختياري) |
| projectId | String? | المشروع (اختياري) |
| startDate | DateTime | تاريخ البدء |
| endDate | DateTime? | تاريخ الانتهاء (اختياري) |

**مثال عملي**:
- المستخدم "أحمد" له دور "UNIT_ADMIN" في الوحدة "فرع صنعاء"
- المستخدم "فاطمة" لها دور "ACCOUNTANT" في المشروع "مشروع X"
- المستخدم "محمد" له دور "SUPER_ADMIN" على مستوى النظام (بدون تحديد كيان)

---

### 13. نظام المحاسبة

نظام محاسبي كامل يتوافق مع المعايير الدولية (IFRS) والمحلية.

#### 13.1 جدول Account (الحساب)

**الحقول الرئيسية**:

| الحقل | النوع | الوصف |
|------|------|-------|
| id | String (CUID) | المعرف الفريد |
| code | String (Unique) | كود الحساب (مثل: 1-1-001) |
| nameAr | String | الاسم بالعربية |
| nameEn | String | الاسم بالإنجليزية |
| type | AccountType | نوع الحساب |
| category | AccountCategory | تصنيف الحساب |
| parentId | String? | معرف الحساب الأب |
| level | Int | المستوى في الشجرة |
| isActive | Boolean | هل الحساب نشط؟ |
| allowPosting | Boolean | هل يسمح بالترحيل؟ |
| holdingId | String? | الشركة القابضة |
| unitId | String? | الوحدة |

**أنواع الحسابات (AccountType Enum)**:
- **ASSET**: أصول
- **LIABILITY**: خصوم
- **EQUITY**: حقوق الملكية
- **REVENUE**: إيرادات
- **EXPENSE**: مصروفات

**تصنيفات الحسابات (AccountCategory Enum)**:
- **CURRENT_ASSET**: أصول متداولة
- **FIXED_ASSET**: أصول ثابتة
- **CURRENT_LIABILITY**: خصوم متداولة
- **LONG_TERM_LIABILITY**: خصوم طويلة الأجل
- **CAPITAL**: رأس المال
- **OPERATING_REVENUE**: إيرادات تشغيلية
- **OPERATING_EXPENSE**: مصروفات تشغيلية
- **OTHER_REVENUE**: إيرادات أخرى
- **OTHER_EXPENSE**: مصروفات أخرى

#### 13.2 جدول AccountHierarchy (الهيكل الهرمي للحسابات)

جدول مساعد لتسريع الاستعلامات الهرمية.

**الحقول**:
- **ancestorId**: معرف الحساب الجد
- **descendantId**: معرف الحساب الحفيد
- **depth**: عمق العلاقة (0 = نفس الحساب، 1 = ابن مباشر، 2 = حفيد، إلخ)

**فائدة الجدول**: يسمح بالاستعلام السريع عن جميع الحسابات الفرعية لحساب معين، دون الحاجة لاستعلامات متداخلة (Recursive Queries).

#### 13.3 جدول FiscalYear (السنة المالية)

**الحقول الرئيسية**:

| الحقل | النوع | الوصف |
|------|------|-------|
| id | String (CUID) | المعرف الفريد |
| code | String | كود السنة (مثل: FY2025) |
| nameAr | String | الاسم بالعربية |
| nameEn | String | الاسم بالإنجليزية |
| startDate | DateTime | تاريخ البدء |
| endDate | DateTime | تاريخ الانتهاء |
| status | FiscalYearStatus | حالة السنة |
| holdingId | String? | الشركة القابضة |
| unitId | String? | الوحدة |

**حالات السنة المالية (FiscalYearStatus Enum)**:
- **DRAFT**: مسودة
- **ACTIVE**: نشطة
- **CLOSED**: مغلقة
- **LOCKED**: مقفلة (لا يمكن التعديل)

#### 13.4 جدول FiscalPeriod (الفترة المالية)

**الحقول الرئيسية**:

| الحقل | النوع | الوصف |
|------|------|-------|
| id | String (CUID) | المعرف الفريد |
| code | String | كود الفترة (مثل: P01-2025) |
| nameAr | String | الاسم بالعربية |
| nameEn | String | الاسم بالإنجليزية |
| startDate | DateTime | تاريخ البدء |
| endDate | DateTime | تاريخ الانتهاء |
| status | FiscalPeriodStatus | حالة الفترة |
| fiscalYearId | String | معرف السنة المالية |
| periodNumber | Int | رقم الفترة (1-12) |

**حالات الفترة المالية**:
- **OPEN**: مفتوحة (يمكن الترحيل)
- **CLOSED**: مغلقة (لا يمكن الترحيل)
- **LOCKED**: مقفلة (لا يمكن التعديل نهائياً)

#### 13.5 جدول JournalEntry (القيد اليومي)

**الحقول الرئيسية**:

| الحقل | النوع | الوصف |
|------|------|-------|
| id | String (CUID) | المعرف الفريد |
| code | String (Unique) | رقم القيد |
| date | DateTime | تاريخ القيد |
| description | String | وصف القيد |
| reference | String? | المرجع (رقم فاتورة، إلخ) |
| type | JournalEntryType | نوع القيد |
| status | JournalEntryStatus | حالة القيد |
| totalDebit | Decimal | إجمالي المدين |
| totalCredit | Decimal | إجمالي الدائن |
| fiscalYearId | String | السنة المالية |
| fiscalPeriodId | String | الفترة المالية |
| holdingId | String? | الشركة القابضة |
| unitId | String? | الوحدة |
| projectId | String? | المشروع |
| createdBy | String | من أنشأ القيد |
| approvedBy | String? | من اعتمد القيد |
| approvedAt | DateTime? | تاريخ الاعتماد |

**أنواع القيود (JournalEntryType Enum)**:
- **MANUAL**: قيد يدوي
- **SALES**: من فاتورة مبيعات
- **PURCHASE**: من فاتورة مشتريات
- **PAYMENT**: من عملية دفع
- **RECEIPT**: من عملية قبض
- **ADJUSTMENT**: قيد تسوية
- **CLOSING**: قيد إقفال

**حالات القيد**:
- **DRAFT**: مسودة
- **POSTED**: مرحّل
- **APPROVED**: معتمد
- **REVERSED**: معكوس (ملغي)

#### 13.6 جدول JournalEntryLine (سطر القيد)

**الحقول الرئيسية**:

| الحقل | النوع | الوصف |
|------|------|-------|
| id | String (CUID) | المعرف الفريد |
| journalEntryId | String | معرف القيد |
| accountId | String | معرف الحساب |
| description | String? | وصف السطر |
| debit | Decimal | المبلغ المدين |
| credit | Decimal | المبلغ الدائن |
| costCenterId | String? | مركز التكلفة |
| lineNumber | Int | رقم السطر |

**قاعدة القيد المزدوج**: مجموع المدين = مجموع الدائن في كل قيد.

#### 13.7 جدول AccountBalance (رصيد الحساب)

جدول مُحسّن لتخزين أرصدة الحسابات بدلاً من حسابها في كل مرة.

**الحقول**:
- **accountId**: معرف الحساب
- **fiscalPeriodId**: الفترة المالية
- **openingBalance**: الرصيد الافتتاحي
- **totalDebit**: إجمالي المدين
- **totalCredit**: إجمالي الدائن
- **closingBalance**: الرصيد الختامي

**فائدة الجدول**: تسريع التقارير المالية. بدلاً من جمع آلاف القيود، نقرأ الرصيد مباشرة.

#### 13.8 جدول CostCenter (مركز التكلفة)

**الحقول الرئيسية**:

| الحقل | النوع | الوصف |
|------|------|-------|
| id | String (CUID) | المعرف الفريد |
| code | String (Unique) | كود مركز التكلفة |
| nameAr | String | الاسم بالعربية |
| nameEn | String | الاسم بالإنجليزية |
| type | CostCenterType | نوع المركز |
| isActive | Boolean | هل المركز نشط؟ |
| holdingId | String? | الشركة القابضة |
| unitId | String? | الوحدة |
| projectId | String? | المشروع |

**أنواع مراكز التكلفة**:
- **DEPARTMENT**: قسم
- **PROJECT**: مشروع
- **ACTIVITY**: نشاط
- **PRODUCT**: منتج
- **LOCATION**: موقع

---

### 14. نظام المخزون

نظام إدارة مخزون شامل يدعم عمليات معقدة.

#### 14.1 جدول Item (الصنف)

**الحقول الرئيسية**:

| الحقل | النوع | الوصف |
|------|------|-------|
| id | String (CUID) | المعرف الفريد |
| code | String (Unique) | كود الصنف (SKU) |
| nameAr | String | الاسم بالعربية |
| nameEn | String | الاسم بالإنجليزية |
| description | String? | وصف الصنف |
| type | ItemType | نوع الصنف |
| categoryId | String? | التصنيف |
| unitOfMeasure | String | وحدة القياس |
| barcode | String? | الباركود |
| image | String? | صورة الصنف |
| isActive | Boolean | هل الصنف نشط؟ |
| trackSerial | Boolean | تتبع الأرقام التسلسلية؟ |
| trackBatch | Boolean | تتبع الدفعات؟ |
| reorderLevel | Decimal? | حد إعادة الطلب |
| minStockLevel | Decimal? | الحد الأدنى للمخزون |
| maxStockLevel | Decimal? | الحد الأقصى للمخزون |

**أنواع الأصناف (ItemType Enum)**:
- **PRODUCT**: منتج (يُباع ويُشترى)
- **SERVICE**: خدمة (لا مخزون لها)
- **RAW_MATERIAL**: مادة خام
- **FINISHED_GOOD**: منتج نهائي
- **CONSUMABLE**: مادة مستهلكة

#### 14.2 جدول ItemCategory (تصنيف الأصناف)

**الحقول**:
- **id**: المعرف الفريد
- **code**: كود التصنيف
- **nameAr**: الاسم بالعربية
- **nameEn**: الاسم بالإنجليزية
- **parentId**: التصنيف الأب (لبناء شجرة)
- **level**: المستوى في الشجرة

#### 14.3 جدول Warehouse (المستودع)

**الحقول الرئيسية**:

| الحقل | النوع | الوصف |
|------|------|-------|
| id | String (CUID) | المعرف الفريد |
| code | String (Unique) | كود المستودع |
| nameAr | String | الاسم بالعربية |
| nameEn | String | الاسم بالإنجليزية |
| type | WarehouseType | نوع المستودع |
| location | String? | الموقع |
| latitude | Decimal? | خط العرض |
| longitude | Decimal? | خط الطول |
| capacity | Decimal? | السعة التخزينية |
| managerId | String? | مدير المستودع |
| isActive | Boolean | هل المستودع نشط؟ |
| holdingId | String? | الشركة القابضة |
| unitId | String? | الوحدة |

**أنواع المستودعات**:
- **MAIN**: مستودع رئيسي
- **BRANCH**: مستودع فرع
- **TRANSIT**: مستودع عبور
- **VIRTUAL**: مستودع افتراضي

#### 14.4 جدول StockLevel (مستوى المخزون)

جدول مُحسّن لتخزين الكميات الحالية.

**الحقول**:
- **id**: المعرف الفريد
- **itemId**: معرف الصنف
- **warehouseId**: معرف المستودع
- **quantity**: الكمية الحالية
- **reservedQuantity**: الكمية المحجوزة
- **availableQuantity**: الكمية المتاحة (quantity - reservedQuantity)
- **lastMovementDate**: تاريخ آخر حركة
- **lastCountDate**: تاريخ آخر جرد

#### 14.5 جدول StockMovement (حركة المخزون)

**الحقول الرئيسية**:

| الحقل | النوع | الوصف |
|------|------|-------|
| id | String (CUID) | المعرف الفريد |
| code | String (Unique) | رقم الحركة |
| date | DateTime | تاريخ الحركة |
| type | StockMovementType | نوع الحركة |
| itemId | String | معرف الصنف |
| fromWarehouseId | String? | من مستودع |
| toWarehouseId | String? | إلى مستودع |
| quantity | Decimal | الكمية |
| unitCost | Decimal? | تكلفة الوحدة |
| totalCost | Decimal? | التكلفة الإجمالية |
| reference | String? | المرجع |
| notes | String? | ملاحظات |
| createdBy | String | من أنشأ الحركة |

**أنواع الحركات (StockMovementType Enum)**:
- **RECEIPT**: استلام
- **ISSUE**: صرف
- **TRANSFER**: تحويل
- **ADJUSTMENT**: تعديل
- **RETURN**: إرجاع
- **DAMAGE**: تلف
- **LOSS**: فقدان

#### 14.6 جدول StockCount (الجرد)

**الحقول الرئيسية**:
- **id**: المعرف الفريد
- **code**: رقم الجرد
- **date**: تاريخ الجرد
- **warehouseId**: المستودع
- **status**: حالة الجرد (DRAFT, IN_PROGRESS, COMPLETED)
- **countedBy**: من قام بالجرد
- **approvedBy**: من اعتمد الجرد

#### 14.7 جدول StockCountLine (سطر الجرد)

**الحقول**:
- **id**: المعرف الفريد
- **stockCountId**: معرف الجرد
- **itemId**: معرف الصنف
- **systemQuantity**: الكمية في النظام
- **countedQuantity**: الكمية المجردة
- **difference**: الفرق
- **notes**: ملاحظات

---

### 15. نظام المشتريات والمبيعات

أنظمة متكاملة لإدارة دورة المشتريات والمبيعات.

#### 15.1 جدول Supplier (المورد)

**الحقول الرئيسية**:
- **id**: المعرف الفريد
- **code**: كود المورد
- **nameAr**: الاسم بالعربية
- **nameEn**: الاسم بالإنجليزية
- **categoryId**: تصنيف المورد
- **taxNumber**: الرقم الضريبي
- **creditLimit**: حد الائتمان
- **paymentTerms**: شروط الدفع
- **isActive**: هل المورد نشط؟

#### 15.2 جدول Customer (العميل)

**الحقول الرئيسية**:
- **id**: المعرف الفريد
- **code**: كود العميل
- **nameAr**: الاسم بالعربية
- **nameEn**: الاسم بالإنجليزية
- **categoryId**: تصنيف العميل
- **taxNumber**: الرقم الضريبي
- **creditLimit**: حد الائتمان
- **paymentTerms**: شروط الدفع
- **isActive**: هل العميل نشط؟

#### 15.3 جدول PurchaseOrder (أمر الشراء)

**الحقول الرئيسية**:
- **id**: المعرف الفريد
- **code**: رقم أمر الشراء
- **date**: تاريخ الأمر
- **supplierId**: المورد
- **warehouseId**: المستودع المستلم
- **status**: حالة الأمر (DRAFT, SENT, CONFIRMED, RECEIVED, CANCELLED)
- **totalAmount**: المبلغ الإجمالي
- **taxAmount**: مبلغ الضريبة
- **netAmount**: المبلغ الصافي

#### 15.4 جدول PurchaseInvoice (فاتورة الشراء)

**الحقول الرئيسية**:
- **id**: المعرف الفريد
- **code**: رقم الفاتورة
- **date**: تاريخ الفاتورة
- **supplierId**: المورد
- **purchaseOrderId**: أمر الشراء المرتبط
- **status**: حالة الفاتورة (DRAFT, POSTED, PAID, CANCELLED)
- **totalAmount**: المبلغ الإجمالي
- **paidAmount**: المبلغ المدفوع
- **remainingAmount**: المبلغ المتبقي

#### 15.5 جدول SalesOrder (أمر البيع)

**الحقول الرئيسية**:
- **id**: المعرف الفريد
- **code**: رقم أمر البيع
- **date**: تاريخ الأمر
- **customerId**: العميل
- **warehouseId**: المستودع المصدر
- **status**: حالة الأمر (DRAFT, CONFIRMED, DELIVERED, INVOICED, CANCELLED)
- **totalAmount**: المبلغ الإجمالي

#### 15.6 جدول SalesInvoice (فاتورة البيع)

**الحقول الرئيسية**:
- **id**: المعرف الفريد
- **code**: رقم الفاتورة
- **date**: تاريخ الفاتورة
- **customerId**: العميل
- **salesOrderId**: أمر البيع المرتبط
- **status**: حالة الفاتورة (DRAFT, POSTED, PAID, CANCELLED)
- **totalAmount**: المبلغ الإجمالي
- **paidAmount**: المبلغ المدفوع
- **remainingAmount**: المبلغ المتبقي

---

### 16. Prisma ORM والتكامل

SEMOP يستخدم Prisma ORM كطبقة تجريد لقاعدة البيانات.

#### 16.1 لماذا Prisma؟

**Type Safety**: Prisma يولد أنواع TypeScript تلقائياً من Schema، مما يمنع الأخطاء في وقت التطوير.

**Migrations**: نظام migrations قوي يتتبع التغييرات في قاعدة البيانات.

**Query Builder**: بناء استعلامات معقدة بطريقة آمنة وسهلة القراءة.

**Relations**: إدارة العلاقات بين الجداول بشكل تلقائي.

#### 16.2 إحصائيات قاعدة البيانات

| المقياس | العدد |
|---------|-------|
| عدد النماذج (Models) | 52 |
| عدد Enums | 29 |
| عدد العلاقات | 150+ |
| عدد الفهارس (Indexes) | 80+ |

#### 16.3 Prisma 7 Migration

في نوفمبر 2025، تم ترقية النظام إلى Prisma 7، والذي قدم تغييرات جذرية:

**Driver Adapters إلزامية**: Prisma 7 يتطلب استخدام Driver Adapter لجميع قواعد البيانات.

**prisma.config.ts**: التكوين انتقل من `schema.prisma` إلى `prisma.config.ts`.

**PrismaClient Initialization**: طريقة جديدة لإنشاء PrismaClient:

```typescript
import { PrismaClient } from '@prisma/client';
import { PrismaPg } from '@prisma/adapter-pg';
import { Pool } from 'pg';

const pool = new Pool({ 
  connectionString: process.env.DATABASE_URL 
});
const adapter = new PrismaPg(pool);
const prisma = new PrismaClient({ adapter });
```

**الفوائد**:
- أداء أفضل
- حجم أصغر
- مرونة أكبر في اختيار Database Driver

---

**تابع في الأقسام التالية...**



## القسم الرابع: Smart Notebook (دفتر الملاحظات الذكي)

### 17. نظرة عامة على Smart Notebook

Smart Notebook هو نظام متكامل لإدارة المعرفة والأفكار والمهام داخل SEMOP. تم تصميمه ليكون أداة شاملة للفرق التقنية والإدارية لتوثيق الأفكار، تتبع التقدم، وإدارة المعرفة المؤسسية.

#### 17.1 الفلسفة والهدف

**لماذا Smart Notebook؟**

في أي مشروع تقني كبير، هناك تحديات مشتركة:
- **تشتت الأفكار**: الأفكار تُطرح في اجتماعات أو محادثات ثم تُنسى
- **فقدان المعرفة**: عندما يغادر موظف، تذهب معه معرفته
- **ضعف التوثيق**: التوثيق غالباً ما يكون قديماً أو غير مكتمل
- **صعوبة التتبع**: صعوبة معرفة ما تم إنجازه وما لا يزال قيد العمل

**الحل**: Smart Notebook يوفر مكاناً واحداً لـ:
- **تسجيل الأفكار**: أي فكرة تُطرح يتم تسجيلها فوراً
- **توثيق المعرفة**: صفحات توثيق منظمة وقابلة للبحث
- **إدارة المهام**: تحويل الأفكار إلى مهام قابلة للتنفيذ
- **تتبع التقدم**: تقارير دورية عن الإنجازات
- **حفظ السياق**: سجل كامل للمحادثات والقرارات

#### 17.2 المكونات الخمسة

Smart Notebook يتكون من خمس وحدات متكاملة:

| الوحدة | الغرض | الحالة |
|--------|-------|--------|
| **Ideas** | تسجيل وإدارة الأفكار | ✅ نشط |
| **Pages** | التوثيق والمعرفة | ✅ نشط |
| **Tasks** | إدارة المهام | ✅ نشط |
| **Reports** | التقارير الدورية | ✅ نشط |
| **ChatLogs** | سجل المحادثات | ✅ نشط |

---

### 18. وحدة Ideas (الأفكار)

وحدة الأفكار هي نقطة البداية لأي ميزة أو تحسين في النظام.

#### 18.1 نموذج البيانات

**جدول Idea**:

| الحقل | النوع | الوصف |
|------|------|-------|
| id | String (CUID) | المعرف الفريد |
| title | String | عنوان الفكرة |
| description | String? | وصف تفصيلي |
| category | IdeaCategory | التصنيف |
| status | IdeaStatus | الحالة |
| priority | Priority | الأولوية |
| tags | String[]? | وسوم للبحث |
| relatedSystem | String? | النظام المرتبط |
| pageId | String? | صفحة التوثيق المرتبطة |
| notes | String? | ملاحظات إضافية |
| createdBy | String | من أنشأ الفكرة |
| updatedBy | String? | من حدّث الفكرة |
| createdAt | DateTime | تاريخ الإنشاء |
| updatedAt | DateTime | تاريخ التحديث |

#### 18.2 التصنيفات (IdeaCategory)

الأفكار تُصنف حسب طبيعتها:

- **FEATURE**: ميزة جديدة كاملة (مثل: إضافة نظام CRM)
- **IMPROVEMENT**: تحسين لميزة موجودة (مثل: تحسين أداء التقارير)
- **BUG_FIX**: إصلاح خطأ في النظام
- **RESEARCH**: بحث أو دراسة (مثل: دراسة جدوى استخدام تقنية معينة)
- **OPTIMIZATION**: تحسين أداء أو كفاءة
- **UI_UX**: تحسينات في واجهة المستخدم
- **DOCUMENTATION**: توثيق أو شرح
- **OTHER**: أخرى

#### 18.3 الحالات (IdeaStatus)

دورة حياة الفكرة:

```
NEW (جديدة)
  ↓
UNDER_REVIEW (قيد الدراسة)
  ↓
ACCEPTED (مقبولة) → CONVERTED (محولة لمهمة)
  أو
REJECTED (مرفوضة) → ARCHIVED (مؤرشفة)
```

**تفصيل الحالات**:

- **NEW**: فكرة جديدة لم تُراجع بعد
- **UNDER_REVIEW**: الفريق يدرس الفكرة
- **ACCEPTED**: الفكرة مقبولة وستُنفذ
- **REJECTED**: الفكرة مرفوضة (مع ذكر السبب في notes)
- **CONVERTED**: تم تحويل الفكرة إلى مهمة قابلة للتنفيذ
- **ARCHIVED**: مؤرشفة (للرجوع إليها لاحقاً)

#### 18.4 الأولويات (Priority)

كل فكرة لها أولوية:

- **LOW**: منخفضة (يمكن تأجيلها)
- **MEDIUM**: متوسطة (مهمة لكن ليست عاجلة)
- **HIGH**: عالية (يجب تنفيذها قريباً)
- **URGENT**: عاجلة (يجب تنفيذها فوراً)

#### 18.5 APIs المتاحة

**Base URL**: `/api/smart-notebook/ideas`

| الطريقة | المسار | الوصف | المصادقة |
|---------|--------|-------|----------|
| POST | `/` | إنشاء فكرة جديدة | ✅ مطلوبة |
| GET | `/` | الحصول على جميع الأفكار (مع تصفية) | ✅ مطلوبة |
| GET | `/statistics` | إحصائيات الأفكار | ✅ مطلوبة |
| GET | `/:id` | الحصول على فكرة واحدة | ✅ مطلوبة |
| PATCH | `/:id` | تحديث فكرة | ✅ مطلوبة |
| DELETE | `/:id` | حذف فكرة | ✅ مطلوبة |
| POST | `/:id/convert-to-task` | تحويل فكرة إلى مهمة | ✅ مطلوبة |
| PATCH | `/:id/status` | تغيير حالة الفكرة | ✅ مطلوبة |

**مثال: إنشاء فكرة جديدة**

```http
POST /api/smart-notebook/ideas
Authorization: Bearer {JWT_TOKEN}
Content-Type: application/json

{
  "title": "إضافة نظام الإشعارات الفورية",
  "description": "نحتاج نظام إشعارات فورية للمستخدمين عند حدوث أحداث مهمة",
  "category": "FEATURE",
  "priority": "HIGH",
  "tags": ["notifications", "real-time", "websocket"],
  "relatedSystem": "Core Services",
  "notes": "يمكن استخدام Socket.io أو Server-Sent Events"
}
```

**مثال: التصفية والبحث**

```http
GET /api/smart-notebook/ideas?status=ACCEPTED&priority=HIGH&category=FEATURE&search=إشعارات&page=1&limit=10&sortBy=createdAt&sortOrder=desc
Authorization: Bearer {JWT_TOKEN}
```

**مثال: تحويل فكرة إلى مهمة**

```http
POST /api/smart-notebook/ideas/clx123abc/convert-to-task
Authorization: Bearer {JWT_TOKEN}
```

هذا سيُنشئ تلقائياً:
1. مهمة جديدة في وحدة Tasks
2. يُحدث حالة الفكرة إلى CONVERTED
3. يربط المهمة بالفكرة الأصلية

#### 18.6 الإحصائيات

**GET /api/smart-notebook/ideas/statistics** يُرجع:

```json
{
  "total": 150,
  "byStatus": {
    "NEW": 30,
    "UNDER_REVIEW": 20,
    "ACCEPTED": 40,
    "REJECTED": 15,
    "CONVERTED": 35,
    "ARCHIVED": 10
  },
  "byCategory": {
    "FEATURE": 50,
    "IMPROVEMENT": 40,
    "BUG_FIX": 30,
    "RESEARCH": 10,
    "OPTIMIZATION": 15,
    "UI_UX": 5
  },
  "byPriority": {
    "LOW": 40,
    "MEDIUM": 60,
    "HIGH": 35,
    "URGENT": 15
  }
}
```

---

### 19. وحدة Pages (صفحات التوثيق)

وحدة Pages هي نظام Wiki داخلي لتوثيق المعرفة المؤسسية.

#### 19.1 نموذج البيانات

**جدول DocumentationPage**:

| الحقل | النوع | الوصف |
|------|------|-------|
| id | String (CUID) | المعرف الفريد |
| title | String | عنوان الصفحة |
| slug | String (Unique) | معرف URL-friendly |
| content | String | المحتوى (Markdown) |
| summary | String? | ملخص قصير |
| category | String? | التصنيف |
| tags | String[]? | وسوم للبحث |
| isPublished | Boolean | هل منشورة؟ |
| version | Int | رقم الإصدار |
| parentId | String? | الصفحة الأب (لبناء شجرة) |
| authorId | String | الكاتب |
| lastEditedBy | String? | آخر من عدّل |
| publishedAt | DateTime? | تاريخ النشر |
| createdAt | DateTime | تاريخ الإنشاء |
| updatedAt | DateTime | تاريخ التحديث |

**جدول PageHistory** (تاريخ التعديلات):

| الحقل | النوع | الوصف |
|------|------|-------|
| id | String (CUID) | المعرف الفريد |
| pageId | String | معرف الصفحة |
| version | Int | رقم الإصدار |
| content | String | المحتوى في هذا الإصدار |
| changeDescription | String? | وصف التغيير |
| editedBy | String | من عدّل |
| editedAt | DateTime | تاريخ التعديل |

**جدول PageComment** (التعليقات):

| الحقل | النوع | الوصف |
|------|------|-------|
| id | String (CUID) | المعرف الفريد |
| pageId | String | معرف الصفحة |
| content | String | محتوى التعليق |
| authorId | String | الكاتب |
| parentId | String? | التعليق الأب (للردود) |
| createdAt | DateTime | تاريخ الإنشاء |
| updatedAt | DateTime | تاريخ التحديث |

**جدول PageLink** (الروابط بين الصفحات):

| الحقل | النوع | الوصف |
|------|------|-------|
| id | String (CUID) | المعرف الفريد |
| sourcePageId | String | الصفحة المصدر |
| targetPageId | String | الصفحة الهدف |
| linkText | String? | نص الرابط |
| createdAt | DateTime | تاريخ الإنشاء |

#### 19.2 الميزات الرئيسية

**Markdown Support**: المحتوى يُكتب بـ Markdown، مما يسمح بـ:
- عناوين وفقرات
- قوائم مرقمة ونقطية
- أكواد برمجية مع syntax highlighting
- جداول
- صور وروابط

**Version Control**: كل تعديل يُحفظ كإصدار جديد. يمكن:
- الرجوع لأي إصدار سابق
- مقارنة الإصدارات
- استعادة إصدار قديم

**Hierarchical Structure**: الصفحات يمكن أن تكون في شجرة:

```
الصفحة الرئيسية
  ├── البنية المعمارية
  │   ├── الطبقة 1: خدمات النواة
  │   ├── الطبقة 2: OCMP
  │   └── الطبقة 3: التطبيقات الرأسية
  ├── دليل المطورين
  │   ├── البدء السريع
  │   └── معايير الكود
  └── APIs
      ├── Authentication
      └── Smart Notebook
```

**Internal Links**: ربط الصفحات ببعضها. عند حذف صفحة، يتم التحذير من الروابط المكسورة.

**Comments & Discussions**: التعليقات على الصفحات للنقاش والتوضيح.

**Search**: بحث نصي كامل في جميع الصفحات.

#### 19.3 APIs المتاحة

**Base URL**: `/api/smart-notebook/pages`

| الطريقة | المسار | الوصف |
|---------|--------|-------|
| POST | `/` | إنشاء صفحة جديدة |
| GET | `/` | الحصول على جميع الصفحات |
| GET | `/search` | البحث في الصفحات |
| GET | `/:id` | الحصول على صفحة واحدة |
| GET | `/:id/history` | تاريخ تعديلات الصفحة |
| GET | `/:id/comments` | تعليقات الصفحة |
| POST | `/:id/comments` | إضافة تعليق |
| PATCH | `/:id` | تحديث صفحة |
| DELETE | `/:id` | حذف صفحة |
| POST | `/:id/publish` | نشر صفحة |
| POST | `/:id/unpublish` | إلغاء نشر صفحة |

**مثال: إنشاء صفحة**

```http
POST /api/smart-notebook/pages
Authorization: Bearer {JWT_TOKEN}
Content-Type: application/json

{
  "title": "دليل استخدام Smart Notebook",
  "slug": "smart-notebook-guide",
  "content": "# دليل استخدام Smart Notebook\n\n## المقدمة\n\nSmart Notebook هو...",
  "summary": "دليل شامل لاستخدام Smart Notebook",
  "category": "Documentation",
  "tags": ["guide", "smart-notebook", "documentation"],
  "isPublished": true
}
```

---

### 20. وحدة Tasks (المهام)

وحدة Tasks هي نظام إدارة مهام متقدم مرتبط بالأفكار والتقارير.

#### 20.1 نموذج البيانات

**جدول Task**:

| الحقل | النوع | الوصف |
|------|------|-------|
| id | String (CUID) | المعرف الفريد |
| title | String | عنوان المهمة |
| description | String? | وصف تفصيلي |
| status | TaskStatus | الحالة |
| priority | Priority | الأولوية |
| source | TaskSource | مصدر المهمة |
| assigneeId | String? | المسؤول |
| reporterId | String | من أنشأ المهمة |
| ideaId | String? | الفكرة المرتبطة |
| pageId | String? | صفحة التوثيق المرتبطة |
| dueDate | DateTime? | تاريخ الاستحقاق |
| estimatedHours | Decimal? | الساعات المقدرة |
| actualHours | Decimal? | الساعات الفعلية |
| tags | String[]? | وسوم |
| notes | String? | ملاحظات |
| createdAt | DateTime | تاريخ الإنشاء |
| updatedAt | DateTime | تاريخ التحديث |
| startedAt | DateTime? | تاريخ البدء |
| completedAt | DateTime? | تاريخ الإكمال |

#### 20.2 الحالات (TaskStatus)

```
TODO (قائمة المهام)
  ↓
IN_PROGRESS (قيد التنفيذ)
  ↓
REVIEW (قيد المراجعة)
  ↓
DONE (مكتملة)

أو

BLOCKED (محظورة) - إذا كانت هناك عوائق
CANCELLED (ملغاة) - إذا تم إلغاء المهمة
```

#### 20.3 مصادر المهام (TaskSource)

- **MANUAL**: مهمة يدوية (أنشأها مستخدم مباشرة)
- **FROM_IDEA**: محولة من فكرة
- **FROM_REPORT**: مذكورة في تقرير
- **FROM_MEETING**: من اجتماع
- **FROM_ISSUE**: من مشكلة أو bug
- **SYSTEM_GENERATED**: أنشأها النظام تلقائياً

#### 20.4 APIs المتاحة

**Base URL**: `/api/smart-notebook/tasks`

| الطريقة | المسار | الوصف |
|---------|--------|-------|
| POST | `/` | إنشاء مهمة جديدة |
| GET | `/` | الحصول على جميع المهام |
| GET | `/my-tasks` | مهامي (المسندة لي) |
| GET | `/statistics` | إحصائيات المهام |
| GET | `/:id` | الحصول على مهمة واحدة |
| PATCH | `/:id` | تحديث مهمة |
| DELETE | `/:id` | حذف مهمة |
| PATCH | `/:id/status` | تغيير حالة المهمة |
| PATCH | `/:id/assign` | تعيين مسؤول |
| POST | `/:id/start` | بدء المهمة |
| POST | `/:id/complete` | إكمال المهمة |

**مثال: إنشاء مهمة**

```http
POST /api/smart-notebook/tasks
Authorization: Bearer {JWT_TOKEN}
Content-Type: application/json

{
  "title": "تطبيق نظام الإشعارات الفورية",
  "description": "تطبيق نظام إشعارات فورية باستخدام Socket.io",
  "priority": "HIGH",
  "source": "FROM_IDEA",
  "ideaId": "clx123abc",
  "assigneeId": "user_xyz",
  "dueDate": "2025-12-31T23:59:59Z",
  "estimatedHours": 40,
  "tags": ["backend", "real-time", "socket.io"]
}
```

**مثال: بدء مهمة**

```http
POST /api/smart-notebook/tasks/clx456def/start
Authorization: Bearer {JWT_TOKEN}
```

هذا سيُحدث:
- الحالة إلى IN_PROGRESS
- startedAt إلى الوقت الحالي

---

### 21. وحدة Reports (التقارير)

وحدة Reports لكتابة تقارير دورية عن التقدم والإنجازات.

#### 21.1 نموذج البيانات

**جدول Report**:

| الحقل | النوع | الوصف |
|------|------|-------|
| id | String (CUID) | المعرف الفريد |
| title | String | عنوان التقرير |
| content | String | المحتوى (Markdown) |
| type | ReportType | نوع التقرير |
| period | String? | الفترة (مثل: أسبوع 45، نوفمبر 2025) |
| summary | String? | ملخص تنفيذي |
| achievements | String[]? | الإنجازات |
| challenges | String[]? | التحديات |
| nextSteps | String[]? | الخطوات القادمة |
| authorId | String | الكاتب |
| isPublished | Boolean | هل منشور؟ |
| publishedAt | DateTime? | تاريخ النشر |
| createdAt | DateTime | تاريخ الإنشاء |
| updatedAt | DateTime | تاريخ التحديث |

#### 21.2 أنواع التقارير (ReportType)

- **ACHIEVEMENT**: تقرير إنجاز (عند إكمال ميزة أو milestone)
- **PHASE**: تقرير مرحلة (عند إكمال مرحلة من المشروع)
- **UPDATE**: تحديث عام (تقرير أسبوعي أو شهري)
- **ANALYSIS**: تحليل (تحليل فني أو تجاري)
- **SUMMARY**: ملخص (ملخص ربع سنوي أو سنوي)
- **TECHNICAL**: تقرير تقني (شرح تقني مفصل)
- **BUSINESS**: تقرير عمل (تقرير للإدارة)
- **OTHER**: أخرى

#### 21.3 APIs المتاحة

**Base URL**: `/api/smart-notebook/reports`

| الطريقة | المسار | الوصف |
|---------|--------|-------|
| POST | `/` | إنشاء تقرير جديد |
| GET | `/` | الحصول على جميع التقارير |
| GET | `/statistics` | إحصائيات التقارير |
| GET | `/:id` | الحصول على تقرير واحد |
| PATCH | `/:id` | تحديث تقرير |
| DELETE | `/:id` | حذف تقرير |
| POST | `/:id/publish` | نشر تقرير |

**مثال: إنشاء تقرير**

```http
POST /api/smart-notebook/reports
Authorization: Bearer {JWT_TOKEN}
Content-Type: application/json

{
  "title": "تقرير التقدم - أسبوع 47، نوفمبر 2025",
  "type": "UPDATE",
  "period": "Week 47, Nov 2025",
  "summary": "تم إكمال Smart Notebook بنجاح وحل مشكلة Prisma 7",
  "achievements": [
    "إكمال Smart Notebook (5 وحدات)",
    "حل مشكلة Prisma 7 Driver Adapter",
    "نشر النظام في Production"
  ],
  "challenges": [
    "مشكلة Prisma 7 استغرقت وقتاً أطول من المتوقع",
    "التوثيق يحتاج تحديث مستمر"
  ],
  "nextSteps": [
    "إكمال دفتر التوثيق الشامل",
    "إضافة اختبارات Unit Tests",
    "تحسين أداء APIs"
  ],
  "content": "# تقرير التقدم\n\n## الإنجازات\n\n...",
  "isPublished": true
}
```

---

### 22. وحدة ChatLogs (سجل المحادثات)

وحدة ChatLogs لحفظ المحادثات المهمة مع الذكاء الاصطناعي أو الفريق.

#### 22.1 نموذج البيانات

**جدول ChatLog**:

| الحقل | النوع | الوصف |
|------|------|-------|
| id | String (CUID) | المعرف الفريد |
| title | String? | عنوان المحادثة |
| sessionId | String | معرف الجلسة |
| messages | Json | المحادثات (JSON array) |
| context | String? | السياق |
| summary | String? | ملخص |
| tags | String[]? | وسوم |
| userId | String | المستخدم |
| relatedIdeaId | String? | فكرة مرتبطة |
| relatedTaskId | String? | مهمة مرتبطة |
| relatedPageId | String? | صفحة مرتبطة |
| createdAt | DateTime | تاريخ الإنشاء |
| updatedAt | DateTime | تاريخ التحديث |

#### 22.2 هيكل messages

```json
[
  {
    "role": "user",
    "content": "كيف أضيف نظام إشعارات فورية؟",
    "timestamp": "2025-11-22T10:00:00Z"
  },
  {
    "role": "assistant",
    "content": "لإضافة نظام إشعارات فورية، يمكنك استخدام Socket.io...",
    "timestamp": "2025-11-22T10:00:05Z"
  },
  {
    "role": "user",
    "content": "هل يمكن استخدام Server-Sent Events بدلاً من Socket.io؟",
    "timestamp": "2025-11-22T10:01:00Z"
  }
]
```

#### 22.3 APIs المتاحة

**Base URL**: `/api/smart-notebook/chat-logs`

| الطريقة | المسار | الوصف |
|---------|--------|-------|
| POST | `/` | حفظ محادثة جديدة |
| GET | `/` | الحصول على جميع المحادثات |
| GET | `/:id` | الحصول على محادثة واحدة |
| PATCH | `/:id` | تحديث محادثة |
| DELETE | `/:id` | حذف محادثة |
| POST | `/:id/link-idea` | ربط محادثة بفكرة |
| POST | `/:id/link-task` | ربط محادثة بمهمة |

**مثال: حفظ محادثة**

```http
POST /api/smart-notebook/chat-logs
Authorization: Bearer {JWT_TOKEN}
Content-Type: application/json

{
  "title": "مناقشة نظام الإشعارات",
  "sessionId": "session_abc123",
  "messages": [
    {
      "role": "user",
      "content": "كيف أضيف نظام إشعارات فورية؟",
      "timestamp": "2025-11-22T10:00:00Z"
    },
    {
      "role": "assistant",
      "content": "لإضافة نظام إشعارات فورية، يمكنك استخدام Socket.io...",
      "timestamp": "2025-11-22T10:00:05Z"
    }
  ],
  "context": "مناقشة حول إضافة نظام إشعارات فورية للمستخدمين",
  "tags": ["notifications", "real-time", "socket.io"]
}
```

---

### 23. التكامل بين الوحدات

Smart Notebook مصمم بحيث تتكامل الوحدات بسلاسة:

#### 23.1 من Idea إلى Task

```
1. مستخدم يُنشئ Idea
2. الفريق يراجع ويقبل الـ Idea
3. يتم تحويل الـ Idea إلى Task
4. Task تُسند لمطور
5. المطور ينفذ ويوثق في Page
6. عند الإكمال، يُكتب Report
```

#### 23.2 من ChatLog إلى Idea

```
1. محادثة مع AI تُنتج فكرة جيدة
2. يتم حفظ المحادثة في ChatLog
3. يتم إنشاء Idea من المحادثة
4. الـ ChatLog يُربط بالـ Idea
```

#### 23.3 من Task إلى Report

```
1. عدة Tasks تُكمل خلال الأسبوع
2. في نهاية الأسبوع، يُكتب Report
3. الـ Report يذكر الـ Tasks المكتملة
4. الـ Report يُنشر للفريق
```

---

**تابع في الأقسام التالية...**



## القسم الخامس: نظام الخرائط

### 24. نظرة عامة على نظام الخرائط

نظام الخرائط SEMOP هو نظام خرائط جغرافية متكامل مصمم خصيصاً لليمن، يوفر إمكانية العمل بدون اتصال بالإنترنت مع صور أقمار صناعية عالية الدقة وحديثة.

#### 24.1 الأهداف الاستراتيجية

**الخصوصية والأمان**: النظام يعمل بشكل كامل بدون اتصال بالإنترنت لضمان حماية البيانات الحساسة والمواقع الاستراتيجية. هذا مهم بشكل خاص للشركات والمؤسسات الحكومية التي تتعامل مع مواقع حساسة.

**الدقة العالية**: صور أقمار صناعية بدقة تصل إلى مستوى Zoom 16 أو أعلى، مما يسمح برؤية تفاصيل دقيقة للمباني والطرق والمنشآت.

**الحداثة**: استخدام صور محدثة وحديثة تعكس الوضع الحالي على الأرض، وليس صور قديمة قد تكون غير دقيقة.

**التكامل الكامل**: الخرائط جزء لا يتجزأ من SEMOP ERP، مما يعني أن كل أصل، عميل، مشروع، أو موقع يمكن ربطه بموقع جغرافي على الخريطة.

**الميزات المتقدمة**: إمكانيات واسعة لإضافة المواقع والملاحظات والصور ورسم المسارات والقياسات وتحليل البيانات الجغرافية.

#### 24.2 التقنيات المستخدمة

| المكون | التقنية | الإصدار | الغرض |
|--------|---------|---------|-------|
| Frontend | Angular | 20 | إطار عمل التطبيق |
| Maps Engine | Leaflet.js | 1.9+ | محرك الخرائط |
| Offline Storage | IndexedDB | - | تخزين Tiles محلياً |
| Backend | NestJS | 10+ | APIs الخلفية |
| Database | PostgreSQL | 14+ | تخزين البيانات |
| File Storage | S3 | - | تخزين الصور |

#### 24.3 الجمهور المستهدف

**المستخدمون النهائيون**: موظفو الشركات الذين يستخدمون النظام في عملهم اليومي لتتبع المواقع والأصول. مثلاً، فني صيانة يحتاج معرفة موقع المولد الذي يجب صيانته.

**مديرو المشاريع**: يحتاجون لتخطيط المشاريع وتتبع تقدمها جغرافياً. مثلاً، مدير مشروع إنشاءات يريد رؤية جميع المواقع على خريطة واحدة.

**المطورون**: الذين يرغبون في تطوير ميزات جديدة أو تخصيص النظام حسب احتياجات محددة.

**مديرو النظام**: المسؤولون عن صيانة وإدارة النظام، بما في ذلك تحديث Tiles وإدارة الصلاحيات.

---

### 25. البنية المعمارية

نظام الخرائط يتكون من أربع طبقات معمارية متكاملة.

#### 25.1 طبقة العرض (Presentation Layer)

**MapsComponent**: المكون الرئيسي الذي يدير عرض الخريطة والتفاعل معها. يستخدم Leaflet.js لعرض الخريطة ويوفر واجهة مستخدم سهلة.

**ControlPanel**: لوحة تحكم توفر أدوات الرسم (مسارات، مناطق)، أدوات القياس (مسافة، مساحة)، وإعدادات الخريطة (نوع الطبقة، الشفافية).

**SidebarPanel**: لوحة جانبية لعرض المواقع المحفوظة، البحث عن مواقع، وتصفية المواقع حسب التصنيف.

**Popups & Dialogs**: نوافذ منبثقة لعرض تفاصيل المواقع، نماذج إضافة مواقع جديدة، ونوافذ تأكيد الإجراءات.

#### 25.2 طبقة الخدمات (Service Layer)

**MapsService**: خدمة رئيسية تدير تهيئة الخريطة، إضافة وإزالة العلامات، التحكم في الطبقات، والتفاعل مع Leaflet API.

**OfflineStorageService**: تدير تحميل وتخزين Tiles في IndexedDB. عند طلب Tile، تبحث أولاً في IndexedDB، وإذا لم تجده تحمله من الإنترنت وتخزنه محلياً.

**DrawingService**: توفر أدوات رسم المسارات (Polylines) والمناطق (Polygons). تستخدم Leaflet.Draw plugin.

**MeasurementService**: تحسب المسافات بين النقاط والمساحات للمناطق. تستخدم خوارزميات جغرافية دقيقة (Haversine formula).

**MapsApiService**: تتواصل مع Backend عبر HTTP لحفظ واسترجاع البيانات (مواقع، ملاحظات، صور، مسارات).

#### 25.3 طبقة البيانات (Data Layer)

**IndexedDB**: قاعدة بيانات محلية في المتصفح تخزن Tiles الخرائط. يمكن تخزين آلاف الـ Tiles لتغطية مناطق واسعة.

**LocalStorage**: يحفظ الإعدادات والتفضيلات (مثل آخر موقع مشاهد، مستوى Zoom المفضل).

**قاعدة البيانات الخلفية**: PostgreSQL تخزن جميع البيانات الدائمة (مواقع، ملاحظات، مسارات، مناطق).

**S3 Storage**: يحفظ الصور والملفات المرفوعة. كل صورة لها URL فريد.

#### 25.4 طبقة الخلفية (Backend Layer)

**Controllers**: تستقبل HTTP requests وترجعها إلى Services. مثلاً، `LocationsController` يعالج طلبات المواقع.

**Services**: تحتوي على المنطق البرمجي. مثلاً، `LocationsService` يتحقق من الصلاحيات، يتحقق من صحة البيانات، ثم يحفظ في قاعدة البيانات.

**Repositories**: تتفاعل مباشرة مع قاعدة البيانات باستخدام Prisma ORM.

**Middleware**: للمصادقة (JWT)، الصلاحيات (RBAC)، وتسجيل الأحداث (Logging).

---

### 26. نموذج البيانات

#### 26.1 جدول locations (المواقع)

| الحقل | النوع | الوصف |
|------|------|-------|
| id | SERIAL PRIMARY KEY | معرف فريد |
| name | VARCHAR(255) NOT NULL | اسم الموقع |
| description | TEXT | وصف تفصيلي |
| latitude | DECIMAL(10,8) NOT NULL | خط العرض |
| longitude | DECIMAL(11,8) NOT NULL | خط الطول |
| icon | VARCHAR(50) | نوع الأيقونة (marker, building, factory, etc.) |
| color | VARCHAR(20) | لون العلامة (hex code) |
| category | VARCHAR(50) | تصنيف (customer, asset, project, warehouse) |
| created_at | TIMESTAMP | تاريخ الإنشاء |
| updated_at | TIMESTAMP | تاريخ التحديث |
| created_by | INTEGER REFERENCES users(id) | المستخدم المنشئ |

**العلاقات**:
- **notes**: علاقة واحد-لكثير مع جدول notes
- **images**: علاقة واحد-لكثير مع جدول images
- **creator**: علاقة كثير-لواحد مع جدول users

#### 26.2 جدول notes (الملاحظات)

| الحقل | النوع | الوصف |
|------|------|-------|
| id | SERIAL PRIMARY KEY | معرف فريد |
| location_id | INTEGER REFERENCES locations(id) ON DELETE CASCADE | الموقع المرتبط |
| content | TEXT NOT NULL | محتوى الملاحظة |
| created_at | TIMESTAMP | تاريخ الإنشاء |
| updated_at | TIMESTAMP | تاريخ التحديث |
| created_by | INTEGER REFERENCES users(id) | المستخدم المنشئ |

#### 26.3 جدول images (الصور)

| الحقل | النوع | الوصف |
|------|------|-------|
| id | SERIAL PRIMARY KEY | معرف فريد |
| location_id | INTEGER REFERENCES locations(id) ON DELETE CASCADE | الموقع المرتبط |
| url | VARCHAR(500) NOT NULL | رابط الصورة على S3 |
| thumbnail_url | VARCHAR(500) | رابط الصورة المصغرة |
| caption | TEXT | وصف الصورة |
| file_size | INTEGER | حجم الملف بالبايت |
| mime_type | VARCHAR(50) | نوع الملف (image/jpeg, image/png) |
| created_at | TIMESTAMP | تاريخ الرفع |
| created_by | INTEGER REFERENCES users(id) | المستخدم الرافع |

#### 26.4 جدول paths (المسارات)

| الحقل | النوع | الوصف |
|------|------|-------|
| id | SERIAL PRIMARY KEY | معرف فريد |
| name | VARCHAR(255) | اسم المسار |
| points | JSONB NOT NULL | نقاط المسار بصيغة JSON |
| distance | DECIMAL(10,2) | المسافة بالمتر |
| color | VARCHAR(20) | لون الخط |
| width | INTEGER | سمك الخط (بالبكسل) |
| created_at | TIMESTAMP | تاريخ الإنشاء |
| created_by | INTEGER REFERENCES users(id) | المستخدم المنشئ |

**مثال على points**:
```json
[
  {"lat": 15.3694, "lng": 44.1910},
  {"lat": 15.3700, "lng": 44.1920},
  {"lat": 15.3710, "lng": 44.1930}
]
```

#### 26.5 جدول polygons (المناطق)

| الحقل | النوع | الوصف |
|------|------|-------|
| id | SERIAL PRIMARY KEY | معرف فريد |
| name | VARCHAR(255) | اسم المنطقة |
| points | JSONB NOT NULL | نقاط المنطقة بصيغة JSON |
| area | DECIMAL(15,2) | المساحة بالمتر المربع |
| color | VARCHAR(20) | لون التعبئة |
| opacity | DECIMAL(3,2) | شفافية التعبئة (0.0 - 1.0) |
| created_at | TIMESTAMP | تاريخ الإنشاء |
| created_by | INTEGER REFERENCES users(id) | المستخدم المنشئ |

---

### 27. APIs الخرائط

جميع APIs تتطلب مصادقة JWT ما لم يُذكر خلاف ذلك.

#### 27.1 APIs المواقع

**Base URL**: `/api/maps/locations`

| الطريقة | المسار | الوصف | Body/Query |
|---------|--------|-------|-----------|
| POST | `/` | إنشاء موقع جديد | name, description, latitude, longitude, icon?, color?, category? |
| GET | `/` | جلب جميع المواقع | ?category=&search=&limit=&offset= |
| GET | `/:id` | جلب موقع محدد | - |
| PUT | `/:id` | تحديث موقع | الحقول المراد تحديثها |
| DELETE | `/:id` | حذف موقع | - |

**مثال: إنشاء موقع**

```http
POST /api/maps/locations
Authorization: Bearer {JWT_TOKEN}
Content-Type: application/json

{
  "name": "مستودع صنعاء الرئيسي",
  "description": "المستودع الرئيسي في صنعاء",
  "latitude": 15.3694,
  "longitude": 44.1910,
  "icon": "warehouse",
  "color": "#FF5733",
  "category": "warehouse"
}
```

**Response**:
```json
{
  "id": 123,
  "name": "مستودع صنعاء الرئيسي",
  "description": "المستودع الرئيسي في صنعاء",
  "latitude": 15.3694,
  "longitude": 44.1910,
  "icon": "warehouse",
  "color": "#FF5733",
  "category": "warehouse",
  "created_at": "2025-11-22T12:00:00Z",
  "created_by": 5
}
```

#### 27.2 APIs الملاحظات

**Base URL**: `/api/maps/notes`

| الطريقة | المسار | الوصف |
|---------|--------|-------|
| POST | `/` | إضافة ملاحظة |
| GET | `/:locationId` | جلب ملاحظات موقع |
| PUT | `/:id` | تحديث ملاحظة |
| DELETE | `/:id` | حذف ملاحظة |

#### 27.3 APIs الصور

**Base URL**: `/api/maps/images`

| الطريقة | المسار | الوصف |
|---------|--------|-------|
| POST | `/` | رفع صورة (multipart/form-data) |
| GET | `/:locationId` | جلب صور موقع |
| DELETE | `/:id` | حذف صورة |

**مثال: رفع صورة**

```http
POST /api/maps/images
Authorization: Bearer {JWT_TOKEN}
Content-Type: multipart/form-data

location_id: 123
file: [binary data]
caption: "صورة المستودع من الخارج"
```

#### 27.4 APIs المسارات

**Base URL**: `/api/maps/paths`

| الطريقة | المسار | الوصف |
|---------|--------|-------|
| POST | `/` | حفظ مسار |
| GET | `/` | جلب جميع المسارات |
| DELETE | `/:id` | حذف مسار |

#### 27.5 APIs المناطق

**Base URL**: `/api/maps/polygons`

| الطريقة | المسار | الوصف |
|---------|--------|-------|
| POST | `/` | حفظ منطقة |
| GET | `/` | جلب جميع المناطق |
| DELETE | `/:id` | حذف منطقة |

---

### 28. الميزات المتقدمة

#### 28.1 العمل الأوفلاين

**كيف يعمل**:

1. **تحميل Tiles**: عند تصفح الخريطة أثناء الاتصال بالإنترنت، يتم تحميل Tiles تلقائياً وتخزينها في IndexedDB.

2. **التخزين الذكي**: النظام يخزن فقط Tiles التي تم عرضها، مما يوفر مساحة التخزين.

3. **Prefetching**: يمكن للمستخدم تحديد منطقة وتحميل جميع Tiles لها مسبقاً (Download for Offline Use).

4. **Fallback**: إذا لم يجد النظام Tile محلياً وكان الإنترنت غير متاح، يعرض Tile placeholder.

**التخزين**:
- كل Tile حجمه تقريباً 10-50 KB
- لتغطية مدينة صنعاء بـ Zoom 16، تحتاج تقريباً 5000 Tile = 250 MB
- IndexedDB يدعم حتى عدة GB من البيانات

#### 28.2 أدوات الرسم

**رسم المسارات (Polylines)**:
- المستخدم ينقر على نقاط على الخريطة
- النظام يرسم خطوط بين النقاط
- يحسب المسافة الإجمالية تلقائياً
- يمكن تخصيص اللون والسمك

**رسم المناطق (Polygons)**:
- المستخدم ينقر على نقاط لتحديد المنطقة
- النظام يرسم منطقة مغلقة
- يحسب المساحة تلقائياً
- يمكن تخصيص اللون والشفافية

#### 28.3 أدوات القياس

**قياس المسافة**:
- المستخدم ينقر على نقطتين أو أكثر
- النظام يحسب المسافة بينهما
- يعرض المسافة بالمتر أو الكيلومتر

**قياس المساحة**:
- المستخدم يحدد منطقة
- النظام يحسب المساحة
- يعرض المساحة بالمتر المربع أو الكيلومتر المربع

**الخوارزميات المستخدمة**:
- **Haversine formula**: لحساب المسافة بين نقطتين على سطح كروي
- **Shoelace formula**: لحساب مساحة مضلع

#### 28.4 التكامل مع SEMOP

**ربط العملاء بالمواقع**:
```typescript
// في نظام CRM
customer.locationId = 123; // ربط العميل بموقع على الخريطة
```

**ربط الأصول بالمواقع**:
```typescript
// في نظام إدارة الأصول
asset.locationId = 456; // ربط الأصل (مثل مولد) بموقع
```

**ربط المشاريع بالمواقع**:
```typescript
// في نظام إدارة المشاريع
project.locationId = 789; // ربط المشروع بموقع
```

**عرض الخريطة في الوحدات الأخرى**:
```html
<!-- في صفحة تفاصيل العميل -->
<app-mini-map [locationId]="customer.locationId"></app-mini-map>
```

---

### 29. دليل الاستخدام

#### 29.1 إضافة موقع جديد

1. افتح نظام الخرائط من القائمة الرئيسية
2. انقر على زر "إضافة موقع" في شريط الأدوات
3. انقر على الخريطة لتحديد الموقع (أو أدخل الإحداثيات يدوياً)
4. أدخل اسم الموقع ووصفه
5. اختر أيقونة ولون
6. اختر تصنيف (عميل، أصل، مشروع، إلخ)
7. انقر "حفظ"

#### 29.2 إضافة ملاحظة لموقع

1. انقر على علامة الموقع على الخريطة
2. في النافذة المنبثقة، انقر على "إضافة ملاحظة"
3. اكتب الملاحظة
4. انقر "حفظ"

#### 29.3 رفع صورة لموقع

1. انقر على علامة الموقع
2. في النافذة المنبثقة، انقر على "رفع صورة"
3. اختر الصورة من جهازك
4. أضف وصف للصورة (اختياري)
5. انقر "رفع"

#### 29.4 رسم مسار

1. انقر على زر "رسم مسار" في شريط الأدوات
2. انقر على نقاط على الخريطة لتحديد المسار
3. انقر مرتين على آخر نقطة لإنهاء الرسم
4. النظام سيعرض المسافة الإجمالية
5. أدخل اسم المسار
6. انقر "حفظ"

#### 29.5 قياس مسافة

1. انقر على زر "قياس" في شريط الأدوات
2. اختر "قياس مسافة"
3. انقر على نقطتين أو أكثر
4. النظام سيعرض المسافة

---

**تابع في الأقسام التالية...**



## القسم السادس: APIs الشاملة

### 30. نظرة عامة على APIs

SEMOP يوفر مجموعة شاملة من RESTful APIs لجميع الوحدات. جميع APIs تستخدم JSON كصيغة البيانات وتتطلب مصادقة JWT ما لم يُذكر خلاف ذلك.

#### 30.1 Base URL

```
Production: https://alabasi.semop.org/api
Development: http://localhost:3000/api
```

#### 30.2 المصادقة

جميع APIs (ما عدا `/auth/login` و `/auth/register`) تتطلب JWT token في header:

```http
Authorization: Bearer {JWT_TOKEN}
```

---

### 31. APIs الأساسية

#### 31.1 Authentication APIs

**Base URL**: `/api/auth`

| الطريقة | المسار | الوصف | Body |
|---------|--------|-------|------|
| POST | `/login` | تسجيل الدخول | username, password |
| POST | `/register` | تسجيل مستخدم جديد | username, password, email, name |
| POST | `/logout` | تسجيل الخروج | - |
| GET | `/me` | الحصول على بيانات المستخدم الحالي | - |
| POST | `/refresh` | تحديث JWT token | refreshToken |
| POST | `/forgot-password` | نسيت كلمة المرور | email |
| POST | `/reset-password` | إعادة تعيين كلمة المرور | token, newPassword |

**مثال: تسجيل الدخول**

```http
POST /api/auth/login
Content-Type: application/json

{
  "username": "admin",
  "password": "password123"
}
```

**Response**:
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "refresh_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": 1,
    "username": "admin",
    "email": "admin@semop.org",
    "name": "مدير النظام",
    "role": "ADMIN"
  }
}
```

#### 31.2 Users APIs

**Base URL**: `/api/users`

| الطريقة | المسار | الوصف |
|---------|--------|-------|
| GET | `/` | جلب جميع المستخدمين |
| GET | `/:id` | جلب مستخدم محدد |
| POST | `/` | إنشاء مستخدم جديد |
| PUT | `/:id` | تحديث مستخدم |
| DELETE | `/:id` | حذف مستخدم |
| PATCH | `/:id/activate` | تفعيل مستخدم |
| PATCH | `/:id/deactivate` | تعطيل مستخدم |

#### 31.3 Roles & Permissions APIs

**Base URL**: `/api/roles` و `/api/permissions`

| الطريقة | المسار | الوصف |
|---------|--------|-------|
| GET | `/roles` | جلب جميع الأدوار |
| POST | `/roles` | إنشاء دور جديد |
| PUT | `/roles/:id` | تحديث دور |
| DELETE | `/roles/:id` | حذف دور |
| GET | `/permissions` | جلب جميع الصلاحيات |
| POST | `/roles/:id/permissions` | إضافة صلاحيات لدور |

---

### 32. APIs المحاسبة

#### 32.1 Accounts APIs (الحسابات)

**Base URL**: `/api/accounts`

| الطريقة | المسار | الوصف |
|---------|--------|-------|
| GET | `/` | جلب شجرة الحسابات |
| GET | `/:id` | جلب حساب محدد |
| POST | `/` | إنشاء حساب جديد |
| PUT | `/:id` | تحديث حساب |
| DELETE | `/:id` | حذف حساب |
| GET | `/:id/balance` | الحصول على رصيد حساب |
| GET | `/:id/transactions` | الحصول على حركات حساب |

#### 32.2 Journal Entries APIs (القيود)

**Base URL**: `/api/journal-entries`

| الطريقة | المسار | الوصف |
|---------|--------|-------|
| GET | `/` | جلب جميع القيود |
| GET | `/:id` | جلب قيد محدد |
| POST | `/` | إنشاء قيد جديد |
| PUT | `/:id` | تحديث قيد |
| DELETE | `/:id` | حذف قيد |
| POST | `/:id/post` | ترحيل قيد |
| POST | `/:id/reverse` | عكس قيد |

**مثال: إنشاء قيد**

```http
POST /api/journal-entries
Authorization: Bearer {JWT_TOKEN}
Content-Type: application/json

{
  "date": "2025-11-22",
  "description": "قيد افتتاحي",
  "fiscalYearId": 1,
  "fiscalPeriodId": 1,
  "lines": [
    {
      "accountId": 10,
      "debit": 100000,
      "credit": 0,
      "description": "رصيد افتتاحي - البنك"
    },
    {
      "accountId": 50,
      "debit": 0,
      "credit": 100000,
      "description": "رصيد افتتاحي - رأس المال"
    }
  ]
}
```

#### 32.3 Fiscal Years & Periods APIs

**Base URL**: `/api/fiscal-years` و `/api/fiscal-periods`

| الطريقة | المسار | الوصف |
|---------|--------|-------|
| GET | `/fiscal-years` | جلب جميع السنوات المالية |
| POST | `/fiscal-years` | إنشاء سنة مالية |
| POST | `/fiscal-years/:id/close` | إقفال سنة مالية |
| GET | `/fiscal-periods` | جلب جميع الفترات المالية |
| POST | `/fiscal-periods/:id/close` | إقفال فترة مالية |

#### 32.4 Cost Centers APIs (مراكز التكلفة)

**Base URL**: `/api/cost-centers`

| الطريقة | المسار | الوصف |
|---------|--------|-------|
| GET | `/` | جلب جميع مراكز التكلفة |
| POST | `/` | إنشاء مركز تكلفة |
| PUT | `/:id` | تحديث مركز تكلفة |
| DELETE | `/:id` | حذف مركز تكلفة |

---

### 33. APIs المخزون

#### 33.1 Items APIs (الأصناف)

**Base URL**: `/api/items`

| الطريقة | المسار | الوصف |
|---------|--------|-------|
| GET | `/` | جلب جميع الأصناف |
| GET | `/:id` | جلب صنف محدد |
| POST | `/` | إنشاء صنف جديد |
| PUT | `/:id` | تحديث صنف |
| DELETE | `/:id` | حذف صنف |
| GET | `/:id/stock` | الحصول على مخزون صنف |
| GET | `/:id/movements` | الحصول على حركات صنف |

#### 33.2 Warehouses APIs (المستودعات)

**Base URL**: `/api/warehouses`

| الطريقة | المسار | الوصف |
|---------|--------|-------|
| GET | `/` | جلب جميع المستودعات |
| POST | `/` | إنشاء مستودع |
| PUT | `/:id` | تحديث مستودع |
| DELETE | `/:id` | حذف مستودع |
| GET | `/:id/stock` | الحصول على مخزون مستودع |

#### 33.3 Stock Movements APIs (حركات المخزون)

**Base URL**: `/api/stock-movements`

| الطريقة | المسار | الوصف |
|---------|--------|-------|
| GET | `/` | جلب جميع الحركات |
| GET | `/:id` | جلب حركة محددة |
| POST | `/` | إنشاء حركة جديدة |
| POST | `/transfer` | تحويل بين مستودعات |
| POST | `/adjustment` | تسوية مخزون |

---

### 34. APIs العملاء والموردين

#### 34.1 Customers APIs (العملاء)

**Base URL**: `/api/customers`

| الطريقة | المسار | الوصف |
|---------|--------|-------|
| GET | `/` | جلب جميع العملاء |
| GET | `/:id` | جلب عميل محدد |
| POST | `/` | إنشاء عميل جديد |
| PUT | `/:id` | تحديث عميل |
| DELETE | `/:id` | حذف عميل |
| GET | `/:id/balance` | الحصول على رصيد عميل |
| GET | `/:id/transactions` | الحصول على معاملات عميل |

#### 34.2 Suppliers APIs (الموردين)

**Base URL**: `/api/suppliers`

| الطريقة | المسار | الوصف |
|---------|--------|-------|
| GET | `/` | جلب جميع الموردين |
| GET | `/:id` | جلب مورد محدد |
| POST | `/` | إنشاء مورد جديد |
| PUT | `/:id` | تحديث مورد |
| DELETE | `/:id` | حذف مورد |
| GET | `/:id/balance` | الحصول على رصيد مورد |

---

### 35. APIs المشاريع

**Base URL**: `/api/projects`

| الطريقة | المسار | الوصف |
|---------|--------|-------|
| GET | `/` | جلب جميع المشاريع |
| GET | `/:id` | جلب مشروع محدد |
| POST | `/` | إنشاء مشروع جديد |
| PUT | `/:id` | تحديث مشروع |
| DELETE | `/:id` | حذف مشروع |
| GET | `/:id/tasks` | الحصول على مهام مشروع |
| POST | `/:id/tasks` | إضافة مهمة لمشروع |
| GET | `/:id/budget` | الحصول على ميزانية مشروع |

---

### 36. معالجة الأخطاء

جميع APIs تستخدم HTTP status codes القياسية:

| الكود | المعنى | الاستخدام |
|------|--------|----------|
| 200 | OK | نجح الطلب |
| 201 | Created | تم إنشاء مورد جديد |
| 204 | No Content | نجح الطلب بدون محتوى |
| 400 | Bad Request | بيانات غير صحيحة |
| 401 | Unauthorized | غير مصرح |
| 403 | Forbidden | ممنوع (لا توجد صلاحيات) |
| 404 | Not Found | المورد غير موجود |
| 409 | Conflict | تعارض (مثل: اسم مستخدم موجود) |
| 422 | Unprocessable Entity | بيانات غير صالحة |
| 500 | Internal Server Error | خطأ في السيرفر |

**مثال على رسالة خطأ**:

```json
{
  "statusCode": 400,
  "message": "Validation failed",
  "errors": [
    {
      "field": "email",
      "message": "البريد الإلكتروني غير صحيح"
    },
    {
      "field": "password",
      "message": "كلمة المرور يجب أن تكون 8 أحرف على الأقل"
    }
  ]
}
```

---

## القسم السابع: دليل المطورين

### 37. البدء السريع

#### 37.1 متطلبات التطوير

**البرمجيات المطلوبة**:
- **Node.js**: 20.x أو أحدث
- **pnpm**: 8.x أو أحدث
- **PostgreSQL**: 14.x أو أحدث
- **Git**: 2.x أو أحدث
- **VS Code**: (موصى به) مع extensions:
  - Prisma
  - ESLint
  - Prettier
  - Angular Language Service

**المعرفة المطلوبة**:
- TypeScript
- NestJS (Backend)
- Angular (Frontend)
- Prisma ORM
- PostgreSQL
- RESTful APIs

#### 37.2 إعداد بيئة التطوير

**1. Clone المشروع**:

```bash
git clone https://github.com/alabasi2025/unified-backend-monorepo.git
cd unified-backend-monorepo
```

**2. تثبيت Dependencies**:

```bash
pnpm install
```

**3. إعداد قاعدة البيانات**:

```bash
# إنشاء قاعدة بيانات PostgreSQL
createdb semop_dev

# إنشاء ملف .env
cp .env.example .env

# تحديث DATABASE_URL في .env
DATABASE_URL="postgresql://user:password@localhost:5432/semop_dev"
```

**4. تشغيل Migrations**:

```bash
pnpm prisma:generate
pnpm prisma:push
```

**5. تشغيل المشروع**:

```bash
# Backend
pnpm nx serve api-gateway

# Frontend (في terminal آخر)
pnpm nx serve frontend
```

**6. الوصول للتطبيق**:
- Backend: http://localhost:3000/api
- Frontend: http://localhost:4200

---

### 38. هيكل المشروع

```
unified-backend-monorepo/
├── apps/
│   ├── api-gateway/          # Backend الرئيسي
│   │   ├── src/
│   │   │   ├── app/          # الوحدات الأساسية
│   │   │   │   ├── auth/
│   │   │   │   ├── users/
│   │   │   │   ├── accounts/
│   │   │   │   └── ...
│   │   │   ├── modules/      # الوحدات الإضافية
│   │   │   │   ├── smart-notebook/
│   │   │   │   ├── warehouses/
│   │   │   │   └── ...
│   │   │   └── main.ts
│   │   └── project.json
│   └── frontend/             # Angular Frontend
│       ├── src/
│       │   ├── app/
│       │   ├── assets/
│       │   └── environments/
│       └── project.json
├── libs/                     # مكتبات مشتركة (إذا وجدت)
├── prisma/
│   ├── schema.prisma         # نموذج قاعدة البيانات
│   └── migrations/           # Migrations
├── docs/                     # الوثائق
├── .env                      # متغيرات البيئة
├── nx.json                   # تكوين Nx
├── package.json
├── pnpm-lock.yaml
└── tsconfig.base.json
```

---

### 39. معايير الكود

#### 39.1 TypeScript

**استخدم Types دائماً**:

```typescript
// ❌ خطأ
function createUser(name, email) {
  // ...
}

// ✅ صحيح
function createUser(name: string, email: string): User {
  // ...
}
```

**استخدم Interfaces للكائنات**:

```typescript
interface CreateUserDto {
  name: string;
  email: string;
  password: string;
}
```

**استخدم Enums للقيم الثابتة**:

```typescript
enum UserRole {
  ADMIN = 'ADMIN',
  USER = 'USER',
  GUEST = 'GUEST'
}
```

#### 39.2 NestJS

**استخدم Dependency Injection**:

```typescript
@Injectable()
export class UsersService {
  constructor(
    private readonly prisma: PrismaService,
    private readonly logger: LoggerService
  ) {}
}
```

**استخدم DTOs للتحقق**:

```typescript
export class CreateUserDto {
  @IsString()
  @IsNotEmpty()
  name: string;

  @IsEmail()
  email: string;

  @IsString()
  @MinLength(8)
  password: string;
}
```

**استخدم Guards للمصادقة**:

```typescript
@Controller('users')
@UseGuards(JwtAuthGuard)
export class UsersController {
  // ...
}
```

#### 39.3 Prisma

**استخدم Transactions للعمليات المترابطة**:

```typescript
await this.prisma.$transaction(async (tx) => {
  const user = await tx.user.create({ data: userData });
  await tx.profile.create({ data: { userId: user.id, ...profileData } });
  return user;
});
```

**استخدم Select لتحديد الحقول**:

```typescript
const user = await this.prisma.user.findUnique({
  where: { id },
  select: {
    id: true,
    name: true,
    email: true,
    // لا تُرجع password
  }
});
```

---

### 40. الاختبارات

#### 40.1 Unit Tests

**مثال: اختبار Service**:

```typescript
describe('UsersService', () => {
  let service: UsersService;
  let prisma: PrismaService;

  beforeEach(async () => {
    const module = await Test.createTestingModule({
      providers: [UsersService, PrismaService],
    }).compile();

    service = module.get<UsersService>(UsersService);
    prisma = module.get<PrismaService>(PrismaService);
  });

  it('should create a user', async () => {
    const userData = {
      name: 'Test User',
      email: 'test@example.com',
      password: 'password123'
    };

    jest.spyOn(prisma.user, 'create').mockResolvedValue({
      id: 1,
      ...userData,
      createdAt: new Date(),
      updatedAt: new Date()
    });

    const result = await service.create(userData);
    expect(result).toHaveProperty('id');
    expect(result.email).toBe(userData.email);
  });
});
```

**تشغيل الاختبارات**:

```bash
# جميع الاختبارات
pnpm nx test api-gateway

# اختبار ملف محدد
pnpm nx test api-gateway --testFile=users.service.spec.ts

# مع coverage
pnpm nx test api-gateway --coverage
```

---

### 41. Git Workflow

#### 41.1 Branching Strategy

```
main (production)
  ↓
develop (development)
  ↓
feature/smart-notebook (ميزة جديدة)
feature/maps-offline (ميزة جديدة)
bugfix/prisma-connection (إصلاح خطأ)
hotfix/security-patch (إصلاح عاجل)
```

#### 41.2 Commit Messages

**استخدم Conventional Commits**:

```
feat: إضافة نظام Smart Notebook
fix: إصلاح مشكلة Prisma 7 Driver Adapter
docs: تحديث دليل التوثيق
refactor: إعادة هيكلة MapsService
test: إضافة اختبارات لـ IdeasController
chore: تحديث dependencies
```

#### 41.3 Pull Request

**قبل إنشاء PR**:
1. تأكد من أن الكود يعمل محلياً
2. شغّل الاختبارات: `pnpm nx test api-gateway`
3. تأكد من أن الكود يتبع معايير Linting: `pnpm nx lint api-gateway`
4. اكتب وصف واضح للتغييرات

---

## القسم الثامن: النشر والصيانة

### 42. نشر Production

#### 42.1 متطلبات السيرفر

**المواصفات الموصى بها**:
- **CPU**: 4 cores أو أكثر
- **RAM**: 8 GB أو أكثر
- **Storage**: 100 GB SSD
- **OS**: Ubuntu 22.04 LTS

**البرمجيات المطلوبة**:
- Node.js 20.x
- PostgreSQL 14.x
- Nginx (كـ reverse proxy)
- PM2 (لإدارة العمليات)
- SSL Certificate (Let's Encrypt)

#### 42.2 خطوات النشر

**1. إعداد السيرفر**:

```bash
# تحديث النظام
sudo apt update && sudo apt upgrade -y

# تثبيت Node.js
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

# تثبيت pnpm
npm install -g pnpm

# تثبيت PM2
npm install -g pm2

# تثبيت PostgreSQL
sudo apt install -y postgresql postgresql-contrib

# تثبيت Nginx
sudo apt install -y nginx
```

**2. Clone المشروع**:

```bash
cd /var/www
git clone https://github.com/alabasi2025/unified-backend-monorepo.git semop
cd semop
```

**3. تثبيت Dependencies**:

```bash
pnpm install --frozen-lockfile
```

**4. إعداد .env**:

```bash
cp .env.example .env
nano .env
```

**5. إعداد قاعدة البيانات**:

```bash
sudo -u postgres psql
CREATE DATABASE semop_production;
CREATE USER semop_user WITH PASSWORD 'strong_password';
GRANT ALL PRIVILEGES ON DATABASE semop_production TO semop_user;
\q

# تشغيل Migrations
pnpm prisma:generate
pnpm prisma:migrate deploy
```

**6. بناء المشروع**:

```bash
pnpm nx build api-gateway --prod
pnpm nx build frontend --prod
```

**7. تشغيل Backend مع PM2**:

```bash
pm2 start dist/apps/api-gateway/main.js --name semop-backend
pm2 save
pm2 startup
```

**8. إعداد Nginx**:

```nginx
server {
    listen 80;
    server_name alabasi.semop.org;

    # Frontend
    location / {
        root /var/www/semop/dist/apps/frontend;
        try_files $uri $uri/ /index.html;
    }

    # Backend API
    location /api {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

**9. تفعيل SSL**:

```bash
sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d alabasi.semop.org
```

#### 42.3 التحديثات

**عند نشر تحديث جديد**:

```bash
cd /var/www/semop
git pull origin main
pnpm install
pnpm prisma:generate
pnpm prisma:migrate deploy
pnpm nx build api-gateway --prod
pm2 restart semop-backend
```

---

### 43. المراقبة والصيانة

#### 43.1 مراقبة PM2

```bash
# عرض حالة العمليات
pm2 status

# عرض Logs
pm2 logs semop-backend

# عرض معلومات تفصيلية
pm2 show semop-backend

# إعادة تشغيل
pm2 restart semop-backend

# إيقاف
pm2 stop semop-backend
```

#### 43.2 Backup قاعدة البيانات

**Backup يدوي**:

```bash
pg_dump -U semop_user -d semop_production > backup_$(date +%Y%m%d).sql
```

**Backup تلقائي (Cron)**:

```bash
# إضافة إلى crontab
crontab -e

# Backup يومي في الساعة 2 صباحاً
0 2 * * * pg_dump -U semop_user -d semop_production > /backups/semop_$(date +\%Y\%m\%d).sql
```

**Restore من Backup**:

```bash
psql -U semop_user -d semop_production < backup_20251122.sql
```

#### 43.3 مراقبة الأداء

**استخدام PM2 Monitoring**:

```bash
pm2 install pm2-logrotate
pm2 set pm2-logrotate:max_size 10M
pm2 set pm2-logrotate:retain 7
```

**استخدام PostgreSQL Logs**:

```bash
# عرض slow queries
sudo tail -f /var/log/postgresql/postgresql-14-main.log | grep "duration"
```

---

**تابع في الأقسام التالية...**



## القسم التاسع: الأمان والصلاحيات

### 44. نظام المصادقة

#### 44.1 JWT Authentication

SEMOP يستخدم JWT (JSON Web Tokens) للمصادقة. عند تسجيل الدخول، يحصل المستخدم على:

**Access Token**: صالح لمدة 15 دقيقة، يُستخدم في كل طلب API.

**Refresh Token**: صالح لمدة 7 أيام، يُستخدم لتجديد Access Token.

**مثال على Token**:

```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOjEsInVzZXJuYW1lIjoiYWRtaW4iLCJyb2xlIjoiQURNSU4iLCJpYXQiOjE3MDA2NTAwMDAsImV4cCI6MTcwMDY1MDkwMH0.abc123...
```

**Payload**:
```json
{
  "sub": 1,
  "username": "admin",
  "role": "ADMIN",
  "iat": 1700650000,
  "exp": 1700650900
}
```

#### 44.2 Password Hashing

كلمات المرور تُخزن بعد تشفيرها باستخدام **bcrypt** مع salt rounds = 10.

```typescript
import * as bcrypt from 'bcrypt';

// عند إنشاء مستخدم
const hashedPassword = await bcrypt.hash(password, 10);

// عند تسجيل الدخول
const isMatch = await bcrypt.compare(password, user.hashedPassword);
```

#### 44.3 CORS

CORS مُفعّل للسماح للـ Frontend بالوصول للـ Backend:

```typescript
app.enableCors({
  origin: ['http://localhost:4200', 'https://alabasi.semop.org'],
  credentials: true,
});
```

---

### 45. نظام الصلاحيات (RBAC)

#### 45.1 الأدوار (Roles)

SEMOP يستخدم Role-Based Access Control (RBAC). الأدوار الأساسية:

| الدور | الوصف | الصلاحيات |
|------|-------|----------|
| **SUPER_ADMIN** | مدير النظام الأعلى | جميع الصلاحيات |
| **ADMIN** | مدير | معظم الصلاحيات |
| **MANAGER** | مدير قسم | صلاحيات القسم |
| **ACCOUNTANT** | محاسب | المحاسبة فقط |
| **WAREHOUSE_KEEPER** | أمين مستودع | المخزون فقط |
| **USER** | مستخدم عادي | صلاحيات محدودة |
| **GUEST** | ضيف | قراءة فقط |

#### 45.2 الصلاحيات (Permissions)

كل دور له مجموعة من الصلاحيات:

| الصلاحية | الوصف |
|---------|-------|
| `users:read` | قراءة المستخدمين |
| `users:create` | إنشاء مستخدمين |
| `users:update` | تحديث مستخدمين |
| `users:delete` | حذف مستخدمين |
| `accounts:read` | قراءة الحسابات |
| `accounts:create` | إنشاء حسابات |
| `journal-entries:post` | ترحيل قيود |
| `fiscal-years:close` | إقفال سنوات مالية |

#### 45.3 Guards

**JwtAuthGuard**: يتحقق من صحة JWT token:

```typescript
@Controller('users')
@UseGuards(JwtAuthGuard)
export class UsersController {
  // جميع endpoints تتطلب مصادقة
}
```

**RolesGuard**: يتحقق من الدور:

```typescript
@Controller('users')
@UseGuards(JwtAuthGuard, RolesGuard)
export class UsersController {
  @Post()
  @Roles('ADMIN', 'SUPER_ADMIN')
  create(@Body() createUserDto: CreateUserDto) {
    // فقط ADMIN و SUPER_ADMIN يمكنهم إنشاء مستخدمين
  }
}
```

**PermissionsGuard**: يتحقق من الصلاحية:

```typescript
@Controller('fiscal-years')
@UseGuards(JwtAuthGuard, PermissionsGuard)
export class FiscalYearsController {
  @Post(':id/close')
  @RequirePermissions('fiscal-years:close')
  close(@Param('id') id: number) {
    // فقط من لديه صلاحية fiscal-years:close
  }
}
```

---

### 46. الأمان في Production

#### 46.1 Environment Variables

**لا تُخزن أسرار في الكود**. استخدم `.env`:

```bash
# .env (لا تُرفع إلى Git!)
DATABASE_URL="postgresql://..."
JWT_SECRET="very_strong_random_secret_key_here"
JWT_REFRESH_SECRET="another_strong_secret"
```

**في .gitignore**:
```
.env
.env.local
.env.production
```

#### 46.2 Rate Limiting

لمنع هجمات DDoS وBrute Force:

```typescript
import { ThrottlerModule } from '@nestjs/throttler';

@Module({
  imports: [
    ThrottlerModule.forRoot({
      ttl: 60,      // 60 ثانية
      limit: 10,    // 10 طلبات كحد أقصى
    }),
  ],
})
export class AppModule {}
```

#### 46.3 Helmet

لحماية Headers:

```typescript
import helmet from 'helmet';

app.use(helmet());
```

#### 46.4 SQL Injection Protection

Prisma يحمي تلقائياً من SQL Injection لأنه يستخدم Parameterized Queries.

**❌ خطأ (عرضة لـ SQL Injection)**:
```typescript
// لا تفعل هذا أبداً!
await prisma.$queryRaw(`SELECT * FROM users WHERE username = '${username}'`);
```

**✅ صحيح**:
```typescript
await prisma.user.findUnique({
  where: { username }
});
```

#### 46.5 XSS Protection

**تنظيف Input**:

```typescript
import * as sanitizeHtml from 'sanitize-html';

const cleanContent = sanitizeHtml(userInput, {
  allowedTags: ['b', 'i', 'em', 'strong', 'a'],
  allowedAttributes: {
    'a': ['href']
  }
});
```

---

## القسم العاشر: الملاحق

### 47. المصطلحات

| المصطلح | الترجمة | الوصف |
|---------|---------|-------|
| **ERP** | Enterprise Resource Planning | نظام تخطيط موارد المؤسسات |
| **OCMP** | Operational Control & Management Platform | منصة التحكم التشغيلي والإدارة |
| **JWT** | JSON Web Token | رمز مصادقة |
| **RBAC** | Role-Based Access Control | التحكم بالوصول المبني على الأدوار |
| **API** | Application Programming Interface | واجهة برمجة التطبيقات |
| **REST** | Representational State Transfer | نمط معماري للـ APIs |
| **ORM** | Object-Relational Mapping | ربط الكائنات بقاعدة البيانات |
| **CRUD** | Create, Read, Update, Delete | العمليات الأساسية |
| **DTO** | Data Transfer Object | كائن نقل البيانات |
| **Tile** | بلاطة الخريطة | صورة صغيرة جزء من الخريطة |
| **Zoom Level** | مستوى التكبير | مستوى التفصيل في الخريطة (0-20) |

---

### 48. الأسئلة الشائعة (FAQ)

#### 48.1 عامة

**س: ما هو SEMOP؟**
ج: SEMOP هو نظام ERP متكامل مصمم خصيصاً لليمن، يوفر إدارة شاملة للمحاسبة، المخزون، المشتريات، المبيعات، والمشاريع.

**س: هل SEMOP مفتوح المصدر؟**
ج: حالياً SEMOP مشروع خاص، لكن قد يتم فتح المصدر في المستقبل.

**س: ما هي التقنيات المستخدمة؟**
ج: Backend: NestJS + Prisma + PostgreSQL. Frontend: Angular 20 + Leaflet.js.

#### 48.2 التثبيت

**س: ما هي متطلبات التشغيل؟**
ج: Node.js 20+, PostgreSQL 14+, 8GB RAM, 100GB Storage.

**س: هل يعمل على Windows؟**
ج: نعم، لكن يُفضل Linux (Ubuntu) للـ Production.

**س: كيف أُحدث النظام؟**
ج: `git pull`, `pnpm install`, `pnpm prisma:migrate deploy`, `pm2 restart`.

#### 48.3 Smart Notebook

**س: ما الفرق بين Idea و Task؟**
ج: Idea هي فكرة أولية قد تُقبل أو تُرفض. Task هي مهمة قابلة للتنفيذ مع مسؤول وتاريخ استحقاق.

**س: هل يمكن تحويل Idea إلى Task؟**
ج: نعم، باستخدام API `POST /api/smart-notebook/ideas/:id/convert-to-task`.

**س: كيف أربط ChatLog بـ Idea؟**
ج: استخدم API `POST /api/smart-notebook/chat-logs/:id/link-idea`.

#### 48.4 الخرائط

**س: هل الخرائط تعمل بدون إنترنت؟**
ج: نعم، بعد تحميل Tiles مسبقاً، يمكن العمل بدون إنترنت.

**س: كيف أُحمل منطقة للعمل الأوفلاين؟**
ج: حدد المنطقة على الخريطة واضغط "Download for Offline Use".

**س: ما هي دقة الصور؟**
ج: Zoom 16 أو أعلى (تفاصيل المباني والطرق واضحة).

#### 48.5 الأمان

**س: هل البيانات آمنة؟**
ج: نعم، نستخدم JWT, bcrypt, HTTPS, CORS, Rate Limiting, وPrisma ORM.

**س: كيف أُغير كلمة المرور؟**
ج: استخدم API `POST /api/auth/change-password` أو من واجهة المستخدم.

**س: ماذا لو نسيت كلمة المرور؟**
ج: استخدم "نسيت كلمة المرور" في صفحة تسجيل الدخول.

---

### 49. الموارد الإضافية

#### 49.1 الوثائق الرسمية

- **NestJS**: https://docs.nestjs.com
- **Prisma**: https://www.prisma.io/docs
- **Angular**: https://angular.dev
- **Leaflet.js**: https://leafletjs.com/reference.html
- **PostgreSQL**: https://www.postgresql.org/docs

#### 49.2 المستودعات

- **Unified Backend Monorepo**: https://github.com/alabasi2025/unified-backend-monorepo
- **Shared Contracts**: https://github.com/alabasi2025/shared-contracts-repo
- **SEMOP (Main)**: https://github.com/alabasi2025/SEMOP

#### 49.3 الاتصال

- **البريد الإلكتروني**: support@semop.org
- **الموقع**: https://semop.org
- **Production**: https://alabasi.semop.org

---

### 50. سجل التغييرات (Changelog)

#### الإصدار 1.0.0 (2025-11-22)

**الميزات الجديدة**:
- ✅ إطلاق Smart Notebook (5 وحدات)
- ✅ نظام الخرائط الأوفلاين
- ✅ نظام المحاسبة الكامل
- ✅ نظام المخزون
- ✅ نظام المشتريات والمبيعات
- ✅ نظام إدارة المشاريع

**الإصلاحات**:
- ✅ حل مشكلة Prisma 7 Driver Adapter
- ✅ تحسين أداء APIs
- ✅ إصلاح مشاكل CORS

**التحسينات**:
- ✅ تحديث التوثيق الشامل
- ✅ إضافة اختبارات Unit Tests
- ✅ تحسين معالجة الأخطاء

---

### 51. خارطة الطريق (Roadmap)

#### Q1 2026

- [ ] إضافة نظام CRM متقدم
- [ ] تطوير Mobile App (Flutter)
- [ ] إضافة نظام التقارير الذكية (BI)
- [ ] تحسين نظام الخرائط (3D Maps)

#### Q2 2026

- [ ] إضافة نظام HR (الموارد البشرية)
- [ ] تطوير نظام Payroll (الرواتب)
- [ ] إضافة نظام الحضور والانصراف
- [ ] تكامل مع أنظمة خارجية (Zapier, etc.)

#### Q3 2026

- [ ] إضافة نظام E-Commerce
- [ ] تطوير نظام POS (نقاط البيع)
- [ ] إضافة نظام الفواتير الإلكترونية
- [ ] تكامل مع البنوك

#### Q4 2026

- [ ] إضافة AI/ML Features
- [ ] تطوير نظام التنبؤ بالمبيعات
- [ ] إضافة Chatbot ذكي
- [ ] تحسين الأداء والسرعة

---

### 52. الخاتمة

SEMOP ERP هو نظام متكامل وقوي مصمم لتلبية احتياجات الشركات والمؤسسات في اليمن. مع ميزات متقدمة مثل Smart Notebook ونظام الخرائط الأوفلاين، يوفر SEMOP تجربة فريدة ومتميزة.

**النظام الآن في مرحلة Production ويعمل بنجاح على:**
- https://alabasi.semop.org

**تم توثيق النظام بالكامل في هذا الدليل الشامل الذي يغطي:**
- البنية المعمارية (4 طبقات)
- قاعدة البيانات (52 نموذج)
- Smart Notebook (5 وحدات)
- نظام الخرائط (عمل أوفلاين)
- جميع APIs (30+ endpoint)
- دليل المطورين
- دليل النشر والصيانة
- الأمان والصلاحيات

**شكراً لاستخدام SEMOP!**

---

## المراجع

هذا التوثيق تم إنشاؤه بناءً على:

1. الكود المصدري في: https://github.com/alabasi2025/unified-backend-monorepo
2. التوثيق الموجود في: `/docs/`
3. Prisma Schema: `/prisma/schema.prisma`
4. NestJS Documentation: https://docs.nestjs.com
5. Prisma Documentation: https://www.prisma.io/docs
6. Angular Documentation: https://angular.dev
7. Leaflet.js Documentation: https://leafletjs.com

---

**تاريخ الإنشاء**: 2025-11-22  
**الإصدار**: 1.0.0  
**المؤلف**: Manus AI  
**آخر تحديث**: 2025-11-22

---

**© 2025 SEMOP ERP. جميع الحقوق محفوظة.**

